.env
__pycache__/
*.py[cod]
node_modules/
dist/
.codex/
.devgod/
.serena/
.venv/
*.egg-info/
*.db
frontend/*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# T09: a labeled sample is real mail read off the owner's machine and must never
# enter the repository, wherever it lands. The committed schema reference is
# named differently on purpose so this pattern cannot also hide it.
.magic-tower/
labeled-sample.json
.claude/

# companion run transcripts are multi-MB and never belong in history
.companion/runs/*/*.jsonl
.companion/runs/*/tmp/
.companion/build.lock
.companion/status.json
