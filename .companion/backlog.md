# Backlog — found in scope of one run, deliberately not done in it

Out of scope is a scheduling decision, not a decision to forget. Each item names where it came
from and what it blocks.

| # | Item | Why deferred | What it blocks | Size |
|---|------|--------------|----------------|------|
| B1 | `@app.on_event("startup")` → lifespan handler (`backend/app/main.py:46`) | Non-goal of run 2026-09-17T18-57-48 | Any fastapi/starlette upgrade. Removed in Starlette 1.x, so the bump breaks the app, not the tests. Emits 2 DeprecationWarnings per test run today. | S |
| B2 | Bump fastapi 0.115.6 → 0.141 / starlette 0.41.3 → 1.6 | Same run's non-goal; depends on B1 | Clears the anyio 4.15 deprecated-alias tripwire: starlette 0.41.3 imports `anyio.abc.BlockingPortal`, so adding `filterwarnings = ["error"]` would break collection. Fixed upstream by Kludex/starlette#3498. | M |
| B3 | Add a `dead-code:` gate to the contract (vulture, `--min-confidence 80`, baseline 2) | The gate was simply absent in the first contract | Nothing today — but it means no run so far has been checked for dead code. Baseline measured 2026-09-17: 2 findings, both false positives. | S |
| B4 | Decide a whitelist or config for vulture's 60%-confidence noise | Deferred with B3 | Makes the gate usable at default confidence: 80 findings today, nearly all pydantic fields, `model_config`, SQLAlchemy columns and pytest fixtures. | S |
| B5 | `env_file=".env"` is cwd-relative (`backend/app/config.py:13`) | Worked around, not fixed | Running pytest from the repo root loads the root `.env`, whose empty `MICROSOFT_*` values abort collection with 3 pydantic ValidationErrors. The suite must be run from `backend/`. A cwd-independent settings path would remove the trap. | S |
| B6 | Frontend pins every dependency to `latest` and has no lockfile (`frontend/package.json:11-17`) | Backend-only run | Frontend builds are not reproducible; `docker compose build` can change behaviour between runs with no commit. | M |
| B7 | Web dashboard for build visibility | Requested as feedback after run 2026-09-17T18-57-48 | Owner has no view into an autonomous run: what is happening, what is left, what already happened. Next idea for `/companion:explore`. | L |
| B8 | `.claude/` is untracked and un-ignored, so a live agent worktree under `.claude/worktrees/` makes `git status --porcelain` non-empty | Harness artifact, not a build artifact — outside T01's stated scope (tsc leftovers) | AC3 and T01's own `done-when` both end in `test -z "$(git status --porcelain)"`, so they fail spuriously whenever a worktree is live. The manager had to remove the worktree before verifying T01. Flagged in the T04 run notes and still open. | S |
| B9 | No `frontend/.dockerignore`, so `COPY . .` (`frontend/Dockerfile:5`) overlays the host's `node_modules`, `dist/`, `vite.config.js` and `*.tsbuildinfo` on top of what `npm ci` installed | Pre-existing; line 5 unchanged by T01, which was scoped to the lockfile, pins, `npm ci` and the tsc leftovers | Image contents are not fully determined by the lockfile on any machine that has run `npm ci` locally — a glibc `@rolldown/binding-linux-x64-gnu` rides into the musl image alongside the musl binding. Fix: `frontend/.dockerignore` with `node_modules`, `dist`, `*.tsbuildinfo`. Found by the T01 reviewer 2026-09-18. | S |
| B10 | `0001_initial_schema.py:37-43` keys its skip-and-stamp on **table names only**, so a pre-Alembic database holding all four baseline tables but a drifted column set is stamped `0001` and left broken | T02 blocked on this; the fix (compare columns via `compare_metadata`, refuse on any mismatch) is a design change the repair round had no budget left for | Reproduced 2026-09-18: a legacy DB with `priority` dropped → `alembic upgrade head` exit 0, `alembic_version='0001'`, column still absent, `GET /api/work-items` then 500s with `no such column: work_items.priority`. Before T02 the startup `ALTER TABLE` repaired this automatically, so a loud self-healing path became a silent stamp. Narrow population (`priority` and the `ALTER TABLE` both landed in `05bc7f3`, so only a pre-initial-release dev volume can be affected) but the failure mode is silent. | M |
| B11 | `README.md:55-61` and `backend/Dockerfile:22-24` claim an existing volume "needs no manual stamp" and that `0001` "works unchanged on such a database" | Follows B10 — the claim is only false for the drifted-column case B10 describes | An operator or the container CMD follows the documented path and ends up with a stamped, unserviceable database. Correct the wording once B10 makes the check column-aware. | S |
| B12 | Non-conditional, stamp-only baseline `0001` as the alternative design | T02 shipped the conditional skip-and-stamp so the container's unattended `alembic upgrade head` works on an existing volume; the stamp-only design needs an explicit operator step instead | Named by the T02 worker as the road not taken and by the reviewer as the preferable shape. Decide between it and B10's column-aware conditional before building on the chain. | M |
| B13 | shadcn token block: `components.json` sets `"cssVariables": true` but `frontend/src/styles.css` has no `:root`/`.dark`/`@theme` block, so `bg-primary`, `text-primary-foreground`, `border-input`, `ring-ring/50`, `bg-background` emit **0 rules** — a copied `<Button>` renders transparent/colorless | T10 built the foundation; writing tokens there would be dead CSS that T11's `styles.css` replacement deletes | T11. Fix by running `shadcn init` (not `add` — `add` does not write the base token block) or hand-writing the neutral tokens. Found by T10 reviewer, measured on a probe build, 2026-09-18 | S |
| B14 | Preflight gap for button variants: shadcn `variant="ghost"` and `variant="link"` carry no `bg-*` utility, so with preflight dropped they render with UA `ButtonFace` grey and a native outset border; app CSS only resets `.icon,.close` | T10 had to drop the preflight umbrella — it de-bolded every heading (attempt-1 blocker) | T11. Fix is additive and safe: `@import "tailwindcss/preflight.css" layer(base);` — layered, so it loses to unlayered app CSS and changes nothing today. Found by T10 reviewer 2026-09-18 | S |
| B15 | Legacy CSS outspecifies shadcn utilities: `.sync button{background:transparent;border:1px solid #78604a}` is (0,2,0) and beats `.bg-primary` (0,1,0); equal-specificity ties also go to the legacy block since it sits after the imports. Affects buttons inside `.sync`, `.handoff`, `.summary` | T10 does not own the shell rebuild | T11. Fix by wrapping the legacy block in `@layer legacy{...}`. Found by T10 reviewer 2026-09-18 | S |
| B16 | `frontend/src/components/unlock-panel.tsx:11-12` clears the token field on a *failed* unlock: `onUnlock` swallows its own error and returns normally, so `setToken('')` always runs. At base, `setLocalToken('')` sat after the throwing `await`, so a wrong token stayed in the field for correction | Found by the T11 reviewer as an advisory, not a blocking finding; T11's done-when and the layout tests do not cover the failure arm | Owner retypes the whole token after each typo. Fix: have `onUnlock` resolve to a boolean (or rethrow) and only clear on success. Found by the T11 reviewer 2026-09-18 | S |
| B17 | `frontend/package.json:14-35` re-sorts `vite`, `typescript` and three `@types/*` entries alphabetically though none changed version — 6 lines of diff unrelated to the task | Advisory from the T11 reviewer; churn is cosmetic and reverting it post-merge would be its own unrelated diff | Contract's "no unrelated formatting churn" clause. Next task touching `package.json` should insert new deps around the pre-existing entries rather than re-sorting. Found by the T11 reviewer 2026-09-18 | S |
| B18 | The old shell's hero/intro section is gone — the eyebrow copy, "Gather every signal…" and the sample-queue pitch (`styles.css` `.intro` block at base). The Mail layout is an app shell, not a landing page | T11 was scoped to rebuild the shell on shadcn's Mail example; the marketing copy has no place in a three-pane layout and the worker declined to invent one | Nothing technical. But the copy was the only thing explaining the product to a first-time viewer, and it now exists nowhere. Decide whether it wants a deliberate home (docs, README, a landing route) or is dropped on purpose. Raised by the T11 worker 2026-09-18 | S |
| B19 | Frontend bundle is 357.60 kB / 112.76 kB gzip after Radix + cmdk + lucide land (was far smaller); no size budget is configured anywhere, and nobody measures whether `lucide-react`'s per-icon named imports actually tree-shake | T11 legitimately needed these deps; a budget is a separate policy decision and T12/T13 will add more UI before the number settles | No gate catches a bundle regression today. Consider a `build` size assertion once the GUI tasks (T12, T13) are done and the number stops moving. Raised by the T11 worker 2026-09-18 | S |
| B20 | `frontend/src/api.ts:5` exports interface `Evidence`, which is used inside `api.ts` but never imported elsewhere — it is the sole remaining knip finding and the whole TypeScript half of the dead-code baseline | It is the contract's sealed baseline, and T11's file to touch was not `api.ts` (T07/T13 own the envelope change there) | Dropping the `export` keyword takes the TS gate from 1 to 0 and lets the baseline be tightened. One-word change, best folded into T13 which edits `api.ts` anyway. Raised by the T11 worker 2026-09-18 | S |
| B21 | `backend/Dockerfile:22-24` and `README.md:58-59,66-67` overstate the column check: they say a schema "altered by hand" fails, but `_column_drift` compares column **names** only, so a *redefined* column still stamps | T02 reviewer advisory; a docs-only reword, and T02 had already spent its repair round | Operator trust in the unattended `alembic upgrade head` container CMD. Reviewer reproduced: a legacy DB healed by the removed startup DDL (which added `priority` as `VARCHAR(6) NOT NULL DEFAULT 'medium'`) carries a `priority` differing from `0001`'s DDL — extra DEFAULT, no enum CHECK, last ordinal — yet the upgrade exits 0 and stamps `0001`. Name-only is the *correct* behaviour here: a definition-level check would refuse the most likely real volume in existence and crash-loop the CMD. So reword to "gained or lost a baseline column"; do not tighten the code. | S |
| B22 | `backend/tests/test_migrations.py:242` guard test reflects the head-level `MODEL_TABLES` but upgrades only to `0001` | T02 reviewer advisory; it breaks only once a later revision adds a table, which no shipped revision does | **T03 and T07 directly.** Reviewer reproduced: add a table in a new revision and update `MODEL_TABLES` (required by `test_migrations.py:112` and `_schema_drift_from_models`) and the guard raises `sqlalchemy.exc.NoSuchTableError`, a spurious failure in the one test that keeps `BASELINE_COLUMNS` honest. Fix: iterate a test-local constant frozen at `0001`'s four tables. | S |
| B23 | `_column_drift` compares column names only, not types or nullability, so a retyped baseline column is stamped `0001` silently | Deliberate, for the reason in B21 — a definition-level check would falsely refuse pristine pre-Alembic volumes | Nothing today. Recorded so a later reader does not mistake the narrow check for an oversight. Raised as a doubt by the T02 worker and confirmed independently by the reviewer. | M |
| B24 | The companion Bash guard false-positives on T02's own `done-when`: its `DELETE` and `TEST_PATH` patterns both match a chain that runs pytest against a path named `tests` and then removes a temp `.db` file, so the plan's verbatim gate cannot be run as a single Bash call | The guard lives in project-companion, not in this repo | Any future `done-when` that removes a temp file in the same chain as a test path. Worked around twice this session: the manager ran the chain from a script file, and the T02 reviewer substituted a fresh temp path. Both recorded rather than silently skipped. | S |
| B25 | `WorkEvidence.excerpt` (`backend/app/models.py:81`) is still plain `Text`, and `backend/app/api/routes.py:121` copies evidence excerpts into `agent_dispatches.instruction` — two more on-disk cleartext sinks for the same message bodies T03 just sealed | T03's task text scopes the work to `Source.excerpt`; widening it would have pulled `APP_ENCRYPTION_KEY` into `backend/tests/conftest.py` for the existing `test_work_items.py`. Manager ruled it out of scope, worker and reviewer both flagged it independently | **T05 directly** — T05 is the task that carries the excerpt across into `WorkEvidence`, so it is the point where real mail starts landing in this column. Reviewer reproduced 2026-09-18: `POST /api/work-items/{id}/evidence` with `excerpt="evidence-marker-7c2e91"` → 201, marker present in the raw bytes of the SQLite file. It is also `WorkEvidence.excerpt`, not `Source.excerpt`, that the GUI blockquote (`frontend/src/components/mail-detail.tsx:58`) renders. Fix: switch the column to `EncryptedText` with its own Alembic data revision, and decide separately whether dispatch instructions may embed excerpts at all. Until then the contract's "mail and chat content stops sitting in plaintext on disk" is only true of `sources` | M |
| B26 | `registered_kinds()` (`backend/app/services/sync_registry.py:42`) has zero call sites repo-wide — dead code introduced by T04 | Reviewer marked it advisory, not blocking, and the contract's sealed dead-code gate runs vulture at `--min-confidence 80`, where it does not appear; the measured gate stayed at baseline 3 | The contract's quality bar says "no dead code", and this is a genuine unused export the sealed gate honestly misses (`vulture --min-confidence 60` flags it; manager confirmed `grep -rn registered_kinds backend tests` returns the definition only). Fix: delete it, or use it in the `SyncError` message to list known kinds — which would also make the error actionable. Found by the T04 reviewer 2026-09-18 | S |
| B27 | `sync()` still hardwires the Graph-specific parameter `client: GraphClient \| None` and passes it unconditionally to every handler (`backend/app/services/sync.py:61,70`) | Not blocking: a second connector *can* register without editing `sync()` by reusing the `client` slot, which is what AC6 asks for. No mypy/pyright gate exists (`.github/workflows/ci.yml` runs pytest only), so the annotation lie costs nothing today | Handler signatures are shaped by Graph. Reviewer reproduced: a handler `def _sync_jira(settings, db=None, limit=50)` called via `sync(Settings(), kind="jira")` → `TypeError: _sync_jira() got an unexpected keyword argument 'client'`, and `sync(Settings(), kind="jira", jira_client=...)` → `TypeError: sync() got an unexpected keyword argument 'jira_client'`. The `**_: Any` catch-all in `_sync_graph` hides the leak rather than fixing it. Fix: rename to a neutral `transport`, or accept `**extras: Any` in `sync()` and forward it. Found by the T04 reviewer 2026-09-18 | S |
| B28 | Graph handler registration is an import side effect of `sync.py` (`backend/app/services/sync.py:58`), so the registry is empty for anyone importing only `sync_registry` | No current caller hits it — `api/sync.py` imports `sync`, which registers on the way in | Reviewer reproduced: `from app.services.sync_registry import get_sync_handler; get_sync_handler("graph")` → `UnknownSourceKindError`, and `registered_kinds()` → `()`. Any future introspection (an admin endpoint listing available kinds) silently under-reports depending on import order. Fix: have the registry module or app startup import connector modules explicitly. Found by the T04 reviewer 2026-09-18 | S |
| B29 | `register_sync_handler` silently overwrites an existing kind and `unregister_sync_handler` uses `pop(kind, None)` instead of restoring the prior handler (`backend/app/services/sync_registry.py:27,31`) | No test does this today; T04's own fixture registers a fresh fake kind, so nothing is clobbered | Order-dependent suite waiting to happen: a future test that stubs the real kind (`register_sync_handler("graph", stub)` … `unregister_sync_handler("graph")` in `finally`) leaves the registry with **no** `"graph"` handler, so every later test calling `sync(settings)` gets `SyncError: no sync handler registered for source kind 'graph'` instead of `"Microsoft Graph is not configured"`. Fix: have register return/save the previous value and restore it on unregister, or raise on duplicate registration. Found by the T04 reviewer 2026-09-18 | S |
| B30 | Three naming schemes now coexist for "kind" in the backend: the `SourceKind` enum (`outlook_email`/`teams_message`/`manual`), the `external_id` prefixes (`outlook:`, `teams:`), and T04's new connector key `"graph"` | T04's `str` key is correct — see the reviewer's ruling; the vocabulary sprawl is pre-existing and widening it was out of scope | A reader cannot tell which "kind" a given function means without checking. Worth one deliberate naming pass (connector kind vs signal kind) before Jira/Freshservice land and add a fourth. Raised by the T04 reviewer 2026-09-18 | S |
| B31 | Nothing asserts a promoted work item carries its source URL: `_source_url` (`backend/app/services/promotion.py:109-113`) can return `None` for every input with the whole suite green | T05's acceptance criterion is about promoting and ignoring, not link fidelity; both the attempt-1 and attempt-2 reviewers rated it advisory, and the manager confirmed it is not what T05 is blocked on | The GUI's link back to the original message. Manager reproduced twice: replacing the body with `return None` → 43 passed / 87 passed, nothing reddens; replacing it with `return str(value)` (no validation) → 4 failed, so only the *negative* case is pinned. Fix: one assertion in `test_promotion_fills_the_queue_during_a_graph_sync` that the promoted item's `source_url` equals the fixture URL. Found by the T05 attempt-1 reviewer, re-confirmed by the attempt-2 reviewer and the manager 2026-09-18 | S |
| B32 | `should_promote(signal, owner_addresses)` (`backend/app/services/promotion.py:73`) types the owner as `Collection[str]`, which silently accepts a bare `str` and iterates it per character | No caller passes a `str` today — `_sync_graph` passes a tuple — so nothing is wrong at runtime; it is a trap, not a defect | A directly-addressed mail is rejected as "only copied" if anyone passes a single address as a string: `owners` becomes a set of characters and `isdisjoint` is True. Attempt 1's `sync.py` passed exactly that shape to the equivalent parameter, so the next refactor can reintroduce it. Fix: type it `tuple[str, ...]`, or reject `str` explicitly. Found by the T05 attempt-2 reviewer 2026-09-18 | S |
| B33 | Teams signals bypass two rules the heuristic cannot express: the owner's own chat posts promote (Graph's `chatMessage.from.user` is an `identity` carrying `id`/`displayName` only, so `sender` is `None` and no rule compares the poster to the owner), and Teams *system* events (`from` null, `messageType != "message"`) promote as work | T05's named rules are sender/bulk/recipient shaped; a Teams identity comparison is a new rule, and the manager scoped it out of the repair to keep it small | The queue fills with the owner's own messages and join/leave system events once Teams sync runs for real. Fix: select `messageType` in the chat `$select` and skip non-`message` rows; compare `from.user.id` against the profile `id` already fetched in `_sync_graph`. Raised by the T05 attempt-1 reviewer and independently by the attempt-2 worker 2026-09-18 | M |
| B34 | Rule 3's whole premise — that bulk mail is caught by `List-Unsubscribe`/`List-Id`/`List-Post`/`X-Campaign-Id`/`Precedence`/`Auto-Submitted` — rests on Graph returning `internetMessageHeaders` from the *messages list* endpoint under `$select`, which has only ever been exercised against the fake transport | No live tenant is available to any agent in this run, and the contract forbids real mail entering the repository | If Graph ignores `$select` for `internetMessageHeaders` on the list endpoint, `_headers` returns `{}` in production, rule 3 never fires, and rule 2's short marker list is the only bulk defence — while the design docstring explicitly leans on rule 3 to justify leaving `info@`, `news@` and `updates@` alone. Fix: confirm against a real tenant, or fetch headers per message when absent. Disclosed as a doubt by the T05 attempt-2 worker, flagged as compounding by the attempt-2 reviewer 2026-09-18 | M |
| B35 | The fake transport in `backend/tests/test_promotion.py:432-435` tests `"/me/chats" in url` before the chat-messages branch, so it dispatches on a prefix rather than on the messages path | Advisory from the T05 reviewer; `GraphClient` builds `/chats/{id}/messages` today, so every URL the client actually constructs reaches its intended branch and the test proves what it appears to | Graph also accepts `/me/chats/{id}/messages`. If the client ever switched to that valid path, the fake would answer the messages request with `CHATS` rows, `_selected` would strip them to `{"id": "chat-1"}`, `normalize_teams` would yield `sender_kind="user"` and the test would fail `2 == 1` for a reason unrelated to promotion — a loud wrong-reason failure, not a silent pass. Fix: order the messages branch first, or match on `"/messages?"`. Found by the T05 attempt-3 reviewer 2026-09-18 | S |
| B36 | `DELETE /api/work-items/{item_id}` already exists (`backend/app/api/routes.py:43`), and T06's backfill treats "no `WorkItem` row for this `source_external_id`" as "never promoted", so it resurrects any work item the owner deleted by hand | T06's blocking repair is scoped to not overriding the *heuristic's* rejections; hand-deletion is a second, adjacent source of "absent means unseen" | Live on this branch today, not a future T08 concern: the delete route ships now, and both `promote_signals` (T05, unchanged) and the T06 backfill hit it. T08 must implement dismiss as a status transition rather than a hard delete, and should decide whether a hand-deleted item is also a permanent rejection. Found by the T06 attempt-1 reviewer, confirmed by the manager 2026-09-18 | M |
| B37 | `backfill_promoted_sources(db, owner_addresses=())` threads an `owner_addresses` parameter that can never affect the result: `_signal_from_source` never sets `to_recipients`, `_is_only_copied` returns False on an empty recipient list, and no caller passes it (`backend/app/api/sync.py:30`) | Advisory from the T06 attempt-1 reviewer; the sealed dead-code gate at `--min-confidence 80` does not flag a used-looking parameter | The contract's "no dead code" clause. It reads as though rule 4 applies to backfilled rows when it structurally cannot, which is exactly the kind of decoration T05 was blocked twice for. Fix: drop the parameter until the fields it needs are reconstructable. Found by the T06 attempt-1 reviewer 2026-09-18 | S |
| B38 | The empty-table test's docstring claims it pins the cheap `LIMIT 1` path ("no anti-join, no writes"), but deleting that early return leaves it green — on an empty table the `NOT IN` query also returns `[]` and the function still returns 0 | Advisory; the task's "must not slow boot on an empty DB" clause was written for the startup option, and T06 ships an explicit endpoint instead | The performance claim is unpinned, so a later refactor can remove the cheap path silently. Fix: assert on emitted SQL via a `before_cursor_execute` listener counting statements, or drop the overclaiming docstring rather than let it read as evidence. Found by the T06 attempt-1 reviewer 2026-09-18 | S |
| B39 | T02's three legacy-database tests (`backend/tests/test_migrations.py:135,164,178`) build the "pre-Alembic" database with today's `Base.metadata.create_all`, so the moment any column is added to a baseline table, `0001`'s hardcoded `BASELINE_COLUMNS` drift check refuses to stamp and `alembic upgrade head` aborts with `<table> has unexpected [...]` | Out of scope for T06, which needs schema and was pushed into a worse design by it; fixing the test is a T02-shaped change and the manager would not widen a blocked task | Blocks every future schema change, not just T06's — the next task to add a column hits the same wall. The T06 reviewer established the correct fix: build the legacy database from the baseline DDL (`alembic upgrade 0001` against a tmp file, then `DROP TABLE alembic_version`) rather than from live models, which `test_the_alembic_stamp_check_trusts_exactly_the_columns_revision_0001_creates` already proves equivalent. After that, no future revision needs a skip-when-present branch at all. Note the T06 attempt-2 worker's claim that a new *table* hits the same wall is **false** — `0001`'s skip check is scoped to `BASELINE_COLUMNS.keys() & get_table_names()`, so an added table is invisible to it. Found by the T06 attempt-2 worker, corrected and verified by the T06 attempt-2 reviewer 2026-09-18 | M |
| B40 | `POST /api/sync/backfill` has no operator-facing documentation — `README.md:109` mentions `/api/sync` only, and `frontend/src/api.ts:36` calls `/api/sync` only | Raised against T06's diff, which is reset away; it applies to whatever backfill ships on the unblock | An upgrading operator has no way to learn the backfill exists, which matters more under any design where ordering affects the outcome. Fix: one README line beside the `/api/sync` mention. Found by the T06 attempt-2 reviewer 2026-09-18 | S |
| B41 | `SourcePromotion` (`backend/app/models.py:102-118`) is the only child of `sources` with no ORM relationship and no cascade, and nothing in `backend/app` ever issues `PRAGMA foreign_keys=ON`, so its declared `ondelete="CASCADE"` is inert on SQLite | Advisory from the T06 attempt-1 reviewer; it does not affect backfill correctness, because a re-synced message gets a fresh `Source` UUID and so is never matched by an orphaned ledger row | Deleting a `Source` cleans its sibling `source_signal_context` row but orphans the ledger row — the reviewer reproduced `sources: 0 contexts: 0 promotions: 1` after `delete_source`. It leaks rows and breaks the convention `Source.signal_context` sets. Fix: give `Source` the `relationship(..., cascade="all, delete-orphan")` that `signal_context` already has. Found by the T06 attempt-1 reviewer 2026-09-18 | S |
| B42 | `judged_without_context` (`backend/app/services/backfill.py:103`) keys off `signal_context is None`, but `persist_signals` always writes a context row — with all-NULL fields when Graph supplied no sender, since `_address(row["from"])` returns `None` | Advisory; the blocking finding this repair fixes is the same class on a different axis (rule 4), and the manager kept the repair narrow | Such a row is reported to the operator as fully judged while only rules 1 and 5 could actually fire, which is the "must not report 0 silently" clause failing on an axis the envelope does not measure. Fix: make the predicate field-level ("had something to judge on") rather than row-level, which covers this and the rule-4 case together. Found by the T06 attempt-1 reviewer 2026-09-18 | S |
| B43 | `backfill_promoted_sources` (`backend/app/services/backfill.py:98-104`) materializes every `Source` with `list(db.scalars(select(Source)...))` and then lazy-loads `signal_context` per row — an N+1 | Advisory; fine at the current scale, and the manager would not widen a repair that is already blocked on correctness | Grows linearly with the mailbox on a path an operator can press repeatedly. Fix: `selectinload(Source.signal_context)`, one line. Found by the T06 attempt-1 reviewer 2026-09-18 | S |
| B44 | `promoted_without_owner_check` (`backend/app/services/backfill.py:112`) is evaluated *before* `promote_signals` applies its idempotency skip, so a never-judged `Source` whose `external_id` already has a `WorkItem` — reachable today via `POST /api/work-items`, whose `WorkItemCreate.source_external_id` is public — yields `{"considered": 1, "new_work_items": 0, "judged_without_context": 0, "promoted_without_owner_check": 1}`: the count names a promotion that run did not make | Advisory from the T06 repair reviewer, which reproduced it; the manager kept the repair narrow to the blocking rule-4 gap after three attempts on this task | Reads as self-contradictory to an operator (a flagged promotion with zero promotions). Fix: count only signals `promote_signals` actually created an item for — have it return the pair, or re-check `WorkItem` absence in the predicate. **Also note for T08:** its "promote a `Source` the rules missed" endpoint must write a `SourcePromotion` row, or it makes this shape routine rather than exotic. Found by the T06 repair reviewer 2026-09-18 | S |
| B45 | `README.md:113` — `promoted_without_owner_check` counts any promoted mail carrying a To list, including mail addressed **directly to** the owner that a live sync would promote identically; since nearly all real mail has a To list, the count will run close to `new_work_items`, while the bolded adjacent sentence ("mail you were merely Cc'd on is promoted...") invites reading it as a count of Cc-only promotions | Advisory from the T06 repair reviewer; the module docstring already implies the upper-bound reading, and the worker deliberately chose to over-report rather than under-report | An operator sizing the recall gap from the README will overestimate it. Fix: one clause saying the count is an upper bound that includes mail addressed directly to you. Found by the T06 repair reviewer 2026-09-18 | S |
| B46 | Agent worktrees are created inside the repo at `.claude/worktrees/<name>/` and `.claude/` is untracked and not gitignored, so full-repo copies show up in repo-wide greps and in `git status` | Repo hygiene, unrelated to any task's diff; surfaced while reviewing T06 | A reviewer grepping the repo gets hits from stale copies of the code under review, which is how a finding gets attributed to the wrong file. AC3 ("a full build leaves the tree clean — no generated artefact is untracked or unignored") is arguably already violated by it. Fix: add `.claude/` to `.gitignore`. Found by the T06 repair reviewer 2026-09-18 | S |
| B47 | `GET /api/work-items?assigned_agent=` filters on `WorkItem.assigned_agent` (`backend/app/models.py:44`, `backend/app/services/work_items.py:41`), which T07 left unindexed — AC9's wording ("the columns they filter and sort on are indexed") literally covers it | T07's own task text enumerated status/source_kind/updated_at only, so this is the plan's gap rather than the worker's; the filter is unreachable from the GUI and the CLI today | **Scheduled, not deferred: written into T08's body.** Verified on a migrated database — `EXPLAIN QUERY PLAN` for the count gives `SCAN work_items` (full table scan) while the page query uses `ix_work_items_updated_at`. Revision `0006` has shipped, so the fix is `index=True` plus its own revision, not an edit to `0006`. Found by the T07 reviewer and reproduced by the manager 2026-09-18 | S |
| B48 | `backend/app/api/routes.py:14-15` — module-level `LimitQuery`/`OffsetQuery` are single mutable `FieldInfo` objects that FastAPI mutates at import: after `import app.api.routes`, `LimitQuery.alias == 'limit'` permanently (manager reproduced) | Harmless today — both current uses are named `limit`/`offset`, so the injected alias matches the parameter name | A latent trap for the next reuse: a future route declaring `page_size: int = LimitQuery` silently ignores `?page_size=` and reads `?limit=` instead, returning the default. Fix: build `Query(...)` inline per parameter, or use `Annotated[int, Query(...)]`. Found by the T07 reviewer 2026-09-18 | S |
| B49 | `backend/app/services/work_items.py:46,115` — `ORDER BY updated_at DESC` / `observed_at DESC` with `LIMIT/OFFSET` and no tiebreaker column | Not blocking at this contract's scale (single-user local queue, 50 rows a page) | `Source.observed_at` is Graph's second-precision `receivedDateTime`/`createdDateTime`, so exact ties are realistic: two rows sharing a timestamp across a page boundary can appear on both pages or on neither. Fix: append `Model.id` as a deterministic tiebreaker to both `order_by`. Found by the T07 reviewer 2026-09-18 | S |
| B50 | `scripts/pending-work:175,177` — `pending-work list` / `sources` now print T07's `{items,total,limit,offset}` envelope instead of a bare array, and return at most 50 rows with no `--limit`/`--offset` flags | Nothing is falsified today: the CLI prints the decoded JSON straight through, its test mocks the response, and `skills/pending-work/SKILL.md:24-25` only promises "one JSON object" | An agent following the skill silently sees the first 50 of `total` and cannot page past it. Fix: pass-through `--limit`/`--offset` flags and a SKILL.md line naming the envelope. Found by the T07 reviewer 2026-09-18 | S |
| B51 | `GET /api/work-items` with no `status` filter still returns `dismissed` items (`backend/app/services/work_items.py:33-47`), so an item the owner explicitly rejected stays visible to every unfiltered consumer — the GUI queue, `scripts/pending-work list`, and agents | T08's task text required only that dismiss be a status transition surviving re-sync and the backfill, which it does; hiding from lists was never in its body, and T13 owns the GUI | I reproduced it: create → `POST /api/work-items/{id}/dismiss` → 200 → `GET /api/work-items` returns `total: 1`, `statuses: ['dismissed']`. Partially defeats AC10's point in practice even though its letter is met. Fix: exclude `WorkStatus.dismissed` from `list_work_items` when no explicit `status` filter is given, or make T13 filter client-side and say so. Found by T08 reviewer, reproduced by manager 2026-09-18 | S |
| B52 | `frontend/src/api.ts:1` and `frontend/src/lib/work-items.ts:3-4` — `WorkStatus` union is `'pending'\|'in_progress'\|'blocked'\|'done'`, missing `'dismissed'`, which the API now emits | Frontend is T13's scope; widening T08 to touch it was not warranted | Combined with B51, a dismissed item reaching `mail-list.tsx:40` evaluates `statusLabels[item.status]` → `undefined` (blank badge) and `statusTone[item.status]` → `undefined` className. Fix: add `'dismissed'` to the union plus its label and tone. Found by T08 reviewer 2026-09-18 | S |
| B53 | `scripts/pending-work:137,155` — `list --status dismissed` and `update <id> --status dismissed` both exit with argparse "invalid choice"; the agent CLI can neither see nor set the status the API now produces | Out of T08's body, which scoped the two HTTP endpoints only | An agent told to dismiss via the documented CLI path has no way to do it, and cannot filter the status out either. Fix: add `dismissed` to both `choices` tuples. Found by T08 reviewer 2026-09-18 | S |
| B54 | `backend/app/services/promotion.py:177-180` — promoting a source whose work item was dismissed returns 409 "A work item for this source already exists", which does not distinguish "already queued" from "you dismissed this"; the only recovery (`PATCH /api/work-items/{id}` with `{"status":"pending"}`) is undiscoverable from the response, and no test covers dismiss→re-promote | Deliberate design call by the worker, stated in its report and endorsed by the reviewer: the 409 matches the `source_external_id` unique constraint the DB already enforces | Means a dismissed item can never be re-promoted by hand through the promote endpoint — an owner who dismisses by mistake is stuck without knowing why. Fix: distinct 409 detail when the existing item is `dismissed`, plus a test for the dismiss→promote path. Found by T08 reviewer 2026-09-18 | S |
| B55 | B36's other half is still open: `DELETE /api/work-items/{item_id}` still removes the row, so the T06 backfill resurrects a hand-deleted item. T08 closed only the dismiss path | T08's body required the status transition, not a change to delete's semantics; the worker stated this design call explicitly and left `test_backfill_does_not_resurrect_a_work_item_the_owner_hand_deleted` untouched | Recorded so T08 is not mistaken for closing B36 outright. Decide whether a hand-delete is also a permanent rejection or should be steered to dismiss. Found by T08 reviewer 2026-09-18 | S |
| B56 | `backend/tests/test_documented_commands.py:70-77` (`_writes_the_sample`) keys its guard on the literal string `~/.magic-tower/labeled-sample.json`, so a README that respells the uv block's output as `"$HOME/.magic-tower/labeled-sample.json"` drops out of the guarded set entirely | Advisory on a repair already sent back twice; the guard holds for the spelling that actually ships, and widening it was not in T09's four repair items | Manager reproduced: respelling the uv block's `--output` to `"$HOME/..."` *and* deleting its `chmod 700 ~/.magic-tower` line leaves all four docs tests green (`4 passed`) while that block no longer repairs a pre-existing 0755 directory — the exact hole the repair closed. Fix: normalise `$HOME`/`~` (or match on the `.magic-tower` directory component) before matching, and assert every block running `heuristic_export --output` is covered. Found by the T09 repair reviewer, reproduced by the manager 2026-09-18 | S |
| B57 | `backend/tests/test_heuristic_eval.py:268-279` — the utf8 test spawns a subprocess under `LC_ALL=C PYTHONUTF8=0 PYTHONCOERCECLOCALE=0` but never asserts that this actually produced a non-UTF-8 ambient encoding | Advisory; on this host the pins do bite (`locale.getpreferredencoding(False)` is `ANSI_X3.4-1968`, the `path.open().read()` swap goes RED, a no-op edit stays GREEN), so the test is genuine today | On a host where those pins do not yield a non-UTF-8 codec the test goes silently vacuous rather than failing — the same class of quiet decoration T05 and the first T09 repair were sent back for. Fix: have the reader script print `locale.getpreferredencoding(False)` and assert it is not UTF-8. Found by the T09 repair reviewer 2026-09-18 | S |
| B58 | Pointing `app.tools.heuristic_export` at a non-existent SQLite path leaves a 0-byte `.db` file behind before exiting 1 — the sqlite driver creates the file on connect, then the missing-table error fires | Pre-existing behaviour, not introduced or touched by T09's repair diff | Litters the owner's disk with empty databases on exactly the typo the tool's error message is designed to catch, and a second run against the same typo then reports a different error. Fix: probe the path before connecting, or unlink a file the tool created when it exits non-zero. Found by the T09 repair reviewer 2026-09-18 | S |
| B59 | `README.md:125-128` tells the second PC to `cd backend` and then `uv run python -m app.tools.heuristic_export`, but `env_file=".env"` (`backend/app/config.py:12`) is cwd-relative and **`backend/.env` does not exist** — the only `.env` is at the repo root. `app_encryption_key` defaults to `None` (`backend/app/config.py:23`) | Pre-existing; T09's repair items were the compose path, the docs guard, the encoding and the exception width. The manager confirmed the file layout rather than taking it on report | The documented uv sequence will not see `APP_ENCRYPTION_KEY` unless the owner exports it into the environment or creates `backend/.env`, so on the second PC the export cannot decrypt excerpts — and the README's own setup step names a root `.env`. Verified: `backend/.env` absent, root `.env` present. Fix: document `backend/.env`, or make `env_file` resolve relative to the repo root. Found by the T09 repair reviewer, file layout confirmed by the manager 2026-09-18 | M |
| B60 | The README's `0600`/`0700` guarantees for `~/.magic-tower` are POSIX-only; a non-WSL Windows second PC has no such mode semantics, and the documented blocks are bash | Correctly out of scope — the blocks are bash and the owner's second PC platform is not recorded anywhere in the contract | The paragraph reads as an unconditional promise about real mail on a possibly-shared machine. There it is vacuous rather than false, but an owner on native Windows gets no protection while believing they do. Fix: one clause scoping the promise to POSIX hosts, or a PowerShell equivalent. Found by the T09 repair reviewer 2026-09-18 | S |
| B61 | `backend/tests/test_documented_commands.py:81-92` (`_lines_this_test_can_run`) skips every documented line starting `docker `/`cd ` or containing `uv run`, since the test has no daemon and no synced database | Deliberate and stated by the worker; it is what lets the docs tests execute the blocks at all | If directory preparation is ever documented *through* one of those spellings (e.g. inside a `docker compose exec`, or after a `cd`), the tests silently stop guarding it. It fails closed for the spellings foreseen today — an unrecognised prep command simply leaves the mode wrong and the test fails — but the skip list is an unguarded assumption. Fix: assert the skipped lines contain no `mkdir`/`chmod` of the sample directory. Found by the T09 repair worker 2026-09-18 | S |
| B62 | `frontend/src/lib/triage.ts:39` — the grouping key's `.toLowerCase()` is unpinned: deleting it splits `Q3 Budget Review` from `Re: q3 budget review` into two Outlook groups while `npx vitest run -t "triage"` still reports 6 passed / 0 failed. The "first spelling that arrived" label clause at :43 is unpinned for the same reason. One test whose reply differs from the original only in case covers both. Reproduced by the manager, not only reported. |
| B63 | `frontend/src/lib/triage.ts:39` — the T12 tests pin thread *participation*, not thread *identity*: keying on `thread.toLowerCase().split(' ')[0]` still leaves 6 passed / 0 failed, so a first-token grouping that merges "Standup notes" with "Standup notes for Friday" into one group would ship green. Giving the second thread in `triage keeps different threads of one source kind apart` a shared leading word closes it. Reproduced by the manager. |
| B64 | `frontend/src/lib/triage.ts:16` — four more clauses of `threadOf`'s docstring carry no test: `while` → `if` (repeated stripping), deleting `(?:\[\d+\])?` (the documented `RE[2]:` form), deleting `.replace(/\s+/g, ' ')` (inner whitespace), and `.reverse()` on the returned groups (the stated ordering guarantee) each leave the filtered suite green. One table-driven unit test of `threadOf` would cover all four — the function is exported-adjacent and pure, so it needs no render. |
| B65 | Contract AC13 — its verify `cd frontend && npm run test -- --run -t "triage"` matches none of T13's pagination/dismiss/promote tests: measured 6 passed / 14 skipped, all 6 pre-existing T12 tests. So the sealed criterion's stated check for "can promote/dismiss" passes without executing a line of the T13 diff | Contract is sealed; changing it needs `/companion:contract` | T13's own `done-when` (`-t "pagination|dismiss"`, JSON count assertion) does cover it, and I verified it exits 1 when promote is gutted — so the work is proven, but AC13's line reads stronger than it measures. Fix: widen AC13's verify to `-t "triage|pagination|dismiss"` with a count assertion. Found by T13 reviewer, reproduced by manager 2026-09-18 | S |
| B66 | `frontend/src/App.tsx:46,62` — the client-side `status !== 'dismissed'` filter (the B51 workaround) is unpinned: removing it at :46 leaves the done-when at 9 passed / 0 failed, because no mock page ever carries a dismissed item | Pinning it needs a dismissed row in the page-1 fixture, which shifts the counts the T13 tests assert | Fix: give `pagedQueue` one `status:'dismissed'` item in the first page and assert it never renders. Reproduced by manager on attempt 1 (15/0) and by reviewer on attempt 2 (9/0) 2026-09-18 | S |
| B67 | `frontend/src/App.tsx:36-37,50` — `loadMissed` derives its "already promoted" set from the first page only (backend `DEFAULT_PAGE_LIMIT` is 50) while fetching 200 sources, and never re-runs after `loadMore`; past 50 work items, a source whose item sits at offset >= 50 renders under MISSED and Promote returns 409 (B54) with "Could not promote that source." | Out of T13's scope; the task named pagination, promote and dismiss wiring, not cross-page reconciliation | Fix: recompute `missed` against the full loaded `items` after each `loadMore`, or add a server-side promoted flag. Found by both T13 reviewers 2026-09-18 | M |
| B68 | `frontend/src/App.tsx:37,38` — `(page.items ?? [])` is a fallback on a field typed non-optional (`SourcePage.items: Source[]`), and `catch { setMissed([]) }` swallows the failure with no toast or log; together they turn a malformed or failed `/api/sources` response into a silently empty panel | Not a behaviour bug on the happy path | This pair is exactly what *hid* T13 attempt 1's completely unexercised promote path — the mock had no `/api/sources` branch, `page.items` was `undefined`, and the MISSED panel silently never rendered while the suite stayed green at 15. Fix: drop the `?? []` and surface the failure the way `loadMore` and `promote` do. Found by T13 reviewer 2026-09-18 | S |
| B69 | `backend/tests/test_migrations.py:330-345` — T15's lock-in does not guard itself: deleting the `Base.metadata.tables["sources"].append_column(...)` line from `LEGACY_DATABASE_WITH_THE_MODELS_AHEAD_OF_THE_BASELINE` returns the suite to attempt 1's unlocked state with the gate still at **8 passed / 0 failed** | The ordinary regress every guard has — the guard for the guard would need its own guard. The mechanism works today and the proof test's docstring now names the exact regression symptom | A future author who "tidies" the child script back to a plain `_build_baseline_database(path)` call silently removes the only thing that stops the fixture builder regressing to `create_all`. Fix, if ever: have the child print the mutated column list and assert it in the parent, so the mutation itself is asserted rather than assumed. Reproduced by the manager 2026-09-18 | S |
| B70 | `backend/tests/test_migrations.py` cannot run alone: `cd backend && uv run pytest tests/test_migrations.py -q` → exit 1, `2 failed, 6 passed` (`…matches_the_models`, `…takes_the_alembic_stamp_with_its_rows_intact`), because `conftest` imports `Base` but never `app.models`, so `Base.metadata` is empty unless another module pulls in `app.main` | Pre-existing, introduced by T02, untouched by either T15 commit; the `done-when` gate and the full suite both select enough modules to populate the metadata and stay green | Every assertion resting on `Base.metadata` is order-dependent, and one reading of a reversed-order run already cost the T15 repair worker a wasted cycle. Fix: `import app.models  # noqa: F401` at the top of the module or in `conftest`. Found by the T15 repair worker, reproduced by the T15 repair reviewer 2026-09-18 | S |
| B71 | `backend/tests/test_migrations.py:376-377` — the proof test writes `t15_proof_added_column.py` into the **real** `backend/alembic/versions/` and relies on `finally: unlink`. After a hard kill (pytest-timeout's `thread` method calls `os._exit`) the stale file makes the *next* run's `_head_revision()` return the proof id, so the rewritten revision gets `down_revision` pointing at itself and `upgrade head` fails on a cycle | Introduced by T15 attempt 1; the manager scoped the tmp-dir rewrite out of the repair deliberately, because it interacts with the lock-in design and was not what blocked. The window is narrow (the test takes 1.45s against a 30s budget) and it self-heals on the next teardown | Also, while the file exists, a real `alembic upgrade head` against the operator's database applies it — the T15 first reviewer reproduced this: `sources` gained a test-only `nickname` column and `t15_proof_added_column` was stamped as the recorded revision. Fix: `revision_path.unlink(missing_ok=True)` *before* `_head_revision()` as well, or copy `alembic/` + `alembic.ini` into `tmp_path` and run the CLI with `-c` there so the repo tree is never written. Found by both T15 reviewers 2026-09-18 | M |
| B72 | `frontend/src/App.pagination.test.tsx:255` — the rollback test synchronises on toast copy: `await screen.findByText('Could not dismiss that item.')`. Changing that string in `App.tsx:133` reddens `dismiss that the server rejects leaves the unloaded page reachable behind Load more` on a string mismatch while every pagination behaviour is intact | Cosmetic coupling, not a behaviour gap; the test pins the right behaviour via its later assertions | The wait is also near-redundant as a sync point — the optimistic removal is flushed synchronously inside `fireEvent.click`, so the `findByText(firstPage[0].title)` on the next line already waits for the rollback. It does carry one guarantee the title wait alone does not: that the catch path ran rather than the click never firing. Fix: `expect(fetchMock.mock.calls.some(([p]) => String(p).endsWith('/dismiss'))).toBe(true)` — same guarantee, no copy coupling. Found by T13 repair reviewer, flagged independently by the T13 repair worker as its own doubt, 2026-09-18 | S |
| B73 | Companion worktrees are created without `frontend/node_modules`, so the knip half of the sealed `dead-code:` command reports a phantom inflated count unless the agent runs `npm ci` first — the known 3→9 failure mode, hit again this session by the T13 repair worker, which installed before measuring | Process finding, not a repo defect; the playbook is sealed and owner-approved only, so this cannot be self-added | Both T13 workers and both reviewers this session measured correctly only because the manager's prompt said where to measure. Candidate playbook line: *the dead-code gate must be measured in a checkout where dependencies are installed; a worktree without `node_modules` inflates the knip half.* Promote via `/companion:contract` if the owner agrees. Found by T13 repair worker 2026-09-18 | S |
| B74 | `backend/tests/test_end_to_end.py:87-93` — the end-to-end test builds `Settings(app_encryption_key=generate_encryption_key())` for the token store while the `EncryptedText` column type reads the process-wide `APP_ENCRYPTION_KEY` through `app/services/field_crypto.py:38`, so the run executes with **two different encryption keys** — a configuration no deployment can have | Harmless today: the two keys are used by disjoint subsystems (token store vs. column crypto) and the test passes either way | A future divergence between the token-store key and the column key is invisible to the one test whose job is to prove the whole path composes. Fix: use the `encryption_key` fixture pattern already in `tests/test_promotion.py:55-60` (monkeypatch env + `get_settings.cache_clear()`). Found by T14 reviewer 2026-09-18 | S |
| B75 | `backend/tests/test_end_to_end.py:1-21` — the module docstring claims `test_promotion.py` "pins one layer in isolation" and that "none of them proves the layers actually compose". Manager verified this is false: `tests/test_promotion.py:420-461` already drives the identical `GraphClient(token, transport)` seam through the real database and asserts both `source_external_id == "outlook:direct"` and `evidence[0].excerpt == MARKER` | Docstring accuracy only; the test itself is not redundant | The real delta is the **HTTP layer** — route, response schema, envelope — and the manager proved that delta is load-bearing: `exclude=True` on `EvidenceRead.excerpt` (`app/schemas.py:20`) reddens the new test while all 49 `test_promotion.py` tests stay green. Fix: reword the docstring to name the API route and response schema as the added coverage. Found by T14 reviewer, verified by manager 2026-09-18 | S |
| B76 | `backend/tests/test_end_to_end.py:70-83` — `_selected`, `PROFILE`, `INBOX` and the transport dispatch are a second independent copy of `tests/test_promotion.py:442-450`, and this copy silently lacks the `/chats/{id}/messages` branch | Duplication, not a defect; both copies are correct today | Changing the `$select` list in `app/integrations/graph.py` now requires updating two fixtures, and the copy that drifts is the one nothing forces you to look at. Fix: extract the shared fixture transport into a test helper module. Found by T14 reviewer 2026-09-18 | S |
| B77 | `.claude/` is untracked and absent from `.gitignore`, holding ~699 MB of agent worktrees with full source copies | Repo hygiene, outside any task's diff; it has never been committed | It shows up in every `git status --porcelain`, which is the exact signal several task `done-when` gates use to assert a clean tree (T01's, for one). One `git add -A` by a future agent commits 699 MB. Fix: add `.claude/` to `.gitignore`. Found by T14 reviewer 2026-09-18 | S |
| B78 | `backend/app/config.py:82` — the `not value.strip() or` clause of `graph_identifiers_are_not_urls` is dead after T16: the `mode="before"` model validator converts every whitespace-only string on those three fields to `None` first, so `value is not None and not value.strip()` can never be true | T16's scope was making the run command start the app; deleting a now-redundant sub-expression is cleanup, not that task, and it touches a security-adjacent validator | Reproduced by the manager, not only reported: rewriting the condition to `if value is not None and any(char in value for char in "/\\?&#"):` leaves the full suite at **193 passed**, so nothing pins the clause. It is one of the two counts by which the dead-code gate reads 4 against a baseline of 3 (the other is the new `cls`, a pydantic false positive). Fix: drop the `not value.strip() or` clause and note in the docstring that blankness is owned by `blank_optional_settings_are_absent`. Found by T16 reviewer, reproduced by manager 2026-09-19 | S |
| B79 | T16's `done-when` hardcodes `http://127.0.0.1:8787`, so it cannot pass on any host where that port is already bound — including this one, where `headroom.cli proxy --port 8787` (pid 10270) is part of the Claude Code session itself and returns 421. `docker compose up -d --build` then fails at the `web` port bind with `ports are not available: exposing port TCP 127.0.0.1:8787`, **after** the api container has already reported `Healthy` | The gate lives in `plan.md`, which only the manager edits; rewriting an acceptance gate so that it passes is exactly the move a manager should not make unilaterally | This is what blocked T16 despite an approved, container-proven fix. The app itself is fine: with the owner's real root `.env`, base config gives `magic-tower-api-1 Exited (1)` and the T16 commit gives `Healthy`, and the whole chain exits 0 against a free port. Fix, owner's choice: free 8787 before the run, or parameterise the gate (`PORT=${MAGIC_TOWER_PORT:-8787}` in both the compose publish and the curls) so it is runnable on a busy host. Found and reproduced by the manager 2026-09-19 | S |
| B80 | T18's `done-when` is vacuous as written: `A && B && cd backend && C; test $? -ne 0`. The `;` makes `$?` the status of whatever the `&&` chain last ran, so a **failing** clause A or B is what makes the gate report success. Measured at base `35f2048` with nothing implemented: clause A exit 0, clause B **exit 1** (`origin/devgod/run_21fb794e4cf5432aac6836c64fdea769` at `ccd116f`, three commits behind `HEAD`), clause C **exit 0** (the behaviour the task exists to change) — and the literal gate **exit 0**, i.e. green | The gate lives in `plan.md`, which only the manager edits. Rewriting a gate so my own task passes is precisely the move B79 already records a manager declining to make; the same principle binds here even though the rewrite would make the gate *stricter*, not looser | Any T18 verdict resting on the literal gate is worthless — it would have passed before the first line of work. This session verifies the three clauses independently instead (A exit 0, B exit 0, C non-zero) and records each. Fix, owner's choice: replace the `;` with `&&` and invert only clause C — e.g. `… && cd backend && ! MAGIC_TOWER_REQUIRE_SAMPLE=1 uv run python -m app.tools.heuristic_eval --sample /nonexistent.json`. Note the same shape (`cmd; test $? -ne 0`) is worth grepping for in any future gate that asserts a *failure*. Found and reproduced by the manager 2026-09-19 | S |
| B81 | `docs/second-pc.md` — in the corrected provenance section, "no `web` container was ever created" is false. `docker compose up -d` creates the container and then fails to start it | T18 had already used its one repair round; blocking a verified hand-off over one word is disproportionate, and the substantive claim (command exits 1, nothing serves 8787, `api` is left healthy and misleading) is true | Reproduced by the reviewer and then by the manager independently: `docker compose up -d` → exit 1 with `ports are not available`, and `docker compose ps -a` shows `magic-tower-web-1  created  Created`. An earlier manager check saw no `web` container at all and was consistent with the moment it was run, not with the general claim. It matters more than a typo because it sits in the one section whose entire purpose is that every sentence be literally true, and correcting it makes the doc's own warning *stronger*: a reader running `docker compose ps -a` sees a `web` entry and may read it as present-and-fine. Fix: "created but never started". Found by T18 repair reviewer, reproduced by the manager 2026-09-19 | S |
| B82 | `docs/second-pc.md:44-47` — Prerequisites says `LOCAL_API_TOKEN` "gates every write to this instance, including the sync and export steps below", but the export does not check it | Inherited from `d5a7c1d:44`, outside the repair round's ordered work, and it overstates a security control rather than weakening one | Step 5's export runs `docker compose exec api python -m app.tools.heuristic_export`, which imports only `..database`/`..models` (`heuristic_export.py:29-31`) and the reviewer ran it to exit 0 inside the container with no token present. The token really does gate `POST /api/sync` and the GUI session. A reader who believes the export is token-gated may treat container exec access as safer than it is. Fix: drop "and export" from that sentence. Found by T18 repair reviewer 2026-09-19 | S |
| B83 | `backend/tests/test_documented_commands.py:240-244` — `test_every_documented_command_names_its_runner_as_a_prerequisite` tests word *presence* anywhere in the Prerequisites section, not that the tool is stated as required | The guard closes the real gap it was ordered to close (removing `uv` from Prerequisites reddens it by name, manager-verified), and tightening the match is a refinement, not a repair | Two independent probes: the manager neutralised every prose "uv" while leaving `https://docs.astral.sh/uv/` and the guard stayed **green**; the reviewer replaced the bullet with "- you do not need git." and all 6 tests stayed **green**. So a doc can satisfy the guard while telling the reader the opposite. Fix: require the backticked list-item form (`^- \`git\``) rather than `\bgit\b` anywhere in the section. Disclosed by the repair author itself before review. Found by T18 repair author/reviewer, reproduced by the manager 2026-09-19 | S |
| B84 | `backend/tests/test_documented_commands.py:123-134` — `_runner_of` returns `None` (silently exempt, no prerequisite demanded) for several plausible command shapes | Same reason as B83: the guard does its ordered job for all 21 commands the document pastes today; this is about the shapes a *future* doc edit could use | Probed directly by the reviewer: `sudo docker compose up -d`, `python -m app.tools.heuristic_eval` (unversioned `python`), `time uv run x`, `env FOO=1 uv run x`, `cat x \| uv run x`, `uvx ruff`, `python3.12 -c 1` all classify as "needs nothing". The dangerous one is unversioned `python`, which is absent or is Python 2 on plenty of machines. Fix: add `python` to `RUNNERS`, and fail loud on any first word outside `RUNNERS + {cd, cp, mkdir, chmod, rm}` instead of returning `None`. Found by T18 repair reviewer 2026-09-19 | S |
| B85 | Eight stale agent worktrees remain registered under `.claude/worktrees/`, four of them at `35f2048` with no commits (orphans of the two T18 sessions that were interrupted without recording a result) | Not this task's diff, and removing another session's worktree risks discarding unmerged work — two preserved branches (`worktree-agent-aa02c3e50aa3ca413`, `worktree-agent-a2af9b2b1c1e76c3e`) are deliberately kept per T06 | `git worktree list` shows nine entries where one is expected. Compounds B77 (`.claude/` is ~699 MB and ungitignored). Fix: audit each against `git log --oneline 35f2048..<branch>`, remove the ones with no commits, and record the rest in plan.md the way T06's two are. Found by the manager 2026-09-19 | S |
| B86 | `backend/tests/test_docker_compose.py:118-122` — `test_readme_web_port_paragraph_points_at_the_redirect_uri` pins *co-mention* of `WEB_PORT` and `MICROSOFT_REDIRECT_URI` in one paragraph, not what the paragraph says about them. It is a drift-pointer, not a correctness assertion | T16's scope was to stop the two from drifting apart, which it does; asserting on prose content is a further step and risks brittleness of a different kind | Reproduced by the manager, not only reported: replacing the README sentence with the **opposite** advice (``MICROSOFT_REDIRECT_URI` needs no change; it follows `WEB_PORT` automatically`) leaves the suite at **5 passed** in that module. The test does redden on revert (verified), so it is not vacuous — it simply cannot catch the README being wrong. Fix, either way is defensible: assert a stable content phrase too (e.g. `"not derived from" in p`), or keep it as a drift-pointer and say so in the docstring so the next reader does not over-trust it. Found by T16's second reviewer, reproduced by the manager 2026-09-19 | S |
| B87 | `backend/tests/test_docker_compose.py:57` — literal `"/dev/null"` rather than `os.devnull`; on Windows-native Python `docker compose --env-file /dev/null config` fails and all 5 tests in the module error rather than skip | Cosmetic on this project's actual platforms (Linux/WSL) and CI (`ubuntu-latest`, `.github/workflows` lines 9/28/49), so nothing breaks today | One-character-class fix: `os.devnull`. Raised by the repair worker as its own doubt #2 and independently by the second reviewer; neither could reproduce a failure on this platform, and neither can I — this is a portability claim, not a measured defect. Found 2026-09-19 | S |
| B88 | The backend suite now hard-requires the Docker CLI: `backend/tests/test_docker_compose.py` shells out to `docker compose config`. On a machine without it the module gives 5 errors (`FileNotFoundError: 'docker'`), not skips, and the contract's `test:` command fails | Adding `skipif shutil.which("docker") is None` would make the suite pass by skipping, which the playbook explicitly warns against ("If a verify command can pass through a skip or absent-input path, say so at contract time"). Hard failure is the honest behaviour; the right fix is to name Docker in the contract's **Environment facts**, and the contract is sealed — changing it needs `/companion:contract` | Blocks nothing today: CI is `ubuntu-latest` with Docker preinstalled and `docker compose config` needs no daemon, so all three workflow jobs are unaffected. It hits a contributor without Docker, who currently gets an unexplained `FileNotFoundError`. Manager declined the reviewer's `skipif` suggestion deliberately. Found by T16's first reviewer 2026-09-19 | S |
| B89 | `.env.example:10` ships `MICROSOFT_REDIRECT_URI=http://localhost:8787/api/auth/callback` with no hint that it is coupled to `WEB_PORT`, while `cp .env.example .env` is the first line of the README's run instructions | The repair worker judged a third copy of the caveat to be sprawl, having already added it in the two places that *introduce* a port change (`README.md:98-100` and `:113`). Both reviewers agreed the reader who follows the docs is now fine | Second reviewer confirmed the residual path: `cp .env.example .env` then `WEB_PORT=9191 docker compose up --build` yields a `.env` pinning 8787, so sign-in redirects to 8787 — but only for a reader who ignores both README warnings. Fix if wanted: one comment line above `.env.example:10`. Worker flagged this as the call it was least sure of; recorded rather than quietly made, since the manager does not author the work it verifies. Found 2026-09-19 | S |
| B90 | `docs/second-pc.md:106-113` still presents `ss -ltnp | grep 8787` and freeing the port as the *only* remedy for a busy 8787; after T16 the one-step remedy is `WEB_PORT=9191 docker compose up -d` | The document belongs to T18 and its text is still literally true (the default is unchanged), so this is drift, not error. T16's scope named the README, not this doc | That doc is precisely where a second-PC reader meets the failure, so the two will read inconsistently until one sentence is added. Found by T16's first reviewer 2026-09-19 | S |
| B91 | `backend/tests/test_deadcode_script.py:64` — the backend unit suite now re-invokes **unpinned** `npx --yes knip@latest`, so an upstream knip release that renames an issue key, changes an exit code or alters its JSON shape turns the suite red with no repo change | Pinning the version in the test would make the test disagree with the sealed `dead-code:` line, which is itself `knip@latest`; that divergence is the worse defect, and the seal cannot be edited during a build | Also makes the backend suite network-dependent on first run. Fix, owner's choice: pin both the sealed line and the test to one knip version via `/companion:contract`, or assert against the script's output only and leave `@latest` to the gate. Found by T17 reviewer 2026-09-19 | S |
| B92 | knip's current JSON also emits `catalog`, `catalogReferences` and `optionalPeerDependencies` issue arrays, which neither the sealed 11-key formula, nor `scripts/deadcode.sh`, nor `_KNIP_ISSUE_KEYS` in the test counts | Correct for T17, whose whole requirement is "the same number as the sealed command" — the key list is copied from the seal verbatim, and widening it unilaterally would break that | The shared key list is also `_independent_total()`'s one blind spot: a wrong key *set* is invisible because the script and the test would be wrong together. Worth settling **before T20** widens the gate to CSS. Fix: add the three keys to the sealed line and the script together via `/companion:contract`. Found by T17 reviewer 2026-09-19 | S |
| B93 | After T17 the backend suite hard-requires `frontend/node_modules`: `backend/tests/test_deadcode_script.py` shells out to knip in `frontend/`. On a fresh clone that has not run `npm ci`, two tests fail (manager-reproduced: **2 failed, 2 passed**) rather than skip, so the sealed `test:` command is red until a Node install happens | Exactly parallel to B88 (the suite's Docker CLI dependency). A `skipif` would make the gate's own tests silently not run, which the playbook warns against; hard failure is the honest behaviour. The right fix is naming the dependency in the sealed **Environment facts**, which cannot be edited during a build | T17's repair fixes **CI** by adding Node + `npm ci` to the `test` job, so AC1 and AC15 are green there; the fresh-clone contributor is what remains. Fix: add "the backend suite requires `frontend/node_modules` (`npm ci`) and the Docker CLI" to Environment facts via `/companion:contract`, and have the test's failure message name `npm ci` as the remedy. Found by T17 reviewer, reproduced by the manager 2026-09-19 | S |
| B94 | 14 stale documentation references still advertise Teams ingestion after T19 removed it: `README.md:7,108,113,115`, `docs/second-pc.md:28-34`, `docs/architecture.md:19`, `docs/agent-protocol.md:7`, `skills/pending-work/SKILL.md:3,14,15`, `.codex-plugin/plugin.json:9,14`. `README.md:113` still instructs the reader to register delegated `Chat.Read` in Entra — the exact consent the owner cannot get, which is *why* T19 exists | T19's scope is the code cut; widening a verified diff into 6 doc files during its one repair round would have risked the cut itself. The worker disclosed it as left undone rather than doing it silently | **T21 (dead documentation in the gate) is queued and this is its first real customer** — a check that flags docs referencing things which no longer exist would catch all 14. Until then a reader following README setup asks for a scope the code no longer requests. Found by T19's worker, confirmed by both T19 reviewers and the manager 2026-09-19 | M |
| B95 | `backend/tests/test_remove_teams_migration.py:63-76,248-250` — `_replay_revision_statements` iterates the revision's private `_STATEMENTS` rather than calling `revision.upgrade()`, so if `upgrade()` ever gains a statement outside that tuple, its **second-run** behaviour is unexercised | Partial, not full, tautology: first-run behaviour is still pinned by exact per-table counts at :241-245, and the manager's mutation K (unconditional dispatch delete) reddened this test, so it is load-bearing today | Fix: replay via `revision.upgrade()` under `Operations(MigrationContext.configure(sqlite_conn))` instead of reading the private tuple. Found by T19's repair reviewer 2026-09-19 | S |
| B96 | `tests/agent_protocol/test_pending_work_cli.py:103-117` pins only the *removal* of `teams_message` from `scripts/pending-work:139,146`, not agreement with `SourceKind`. **Reproduced by the manager:** adding `jira_issue` to `SourceKind` without touching either CLI tuple leaves the CLI module **11 passed**. The test's docstring claims it is what keeps the two in step, which overstates it | T19 correctly hard-coded the CLI choices rather than importing `app.models`: `scripts/pending-work` is a standalone stdlib-only executable and deriving would need a `sys.path` insert plus a SQLAlchemy import | **Settle before the Jira connector**, which the contract's Result names as the next kind to plug into the registry — it will add a `SourceKind` member and the CLI will silently not offer it. Fix: a test under `tests/` (which can import both) asserting `set(choices) == {m.value for m in SourceKind}` for both parsers; the CLI itself stays stdlib-only. Found by T19's repair reviewer, reproduced by the manager 2026-09-19 | M |
| B97 | **The contract's `test:` command (AC1) is red on any fresh clone, and has been since T17 landed in `e8b43dc`.** `backend/tests/test_deadcode_script.py` runs `scripts/deadcode.sh` end to end, which runs `frontend/node_modules/.bin/knip`; CI's backend `test` job (`.github/workflows/ci.yml:8-23`) has only `uv sync` — no Node, no `npm ci`, no `node_modules`. **Reproduced by the manager 2026-09-19 on a throwaway clone of base `230b96d` into `/tmp/ci-sim` (`uv sync`, no frontend install): `cd backend && uv run pytest tests ../tests/agent_protocol -q` → exit 1, `2 failed, 216 passed`,** both failures `deadcode.sh: knip exited 127 (not 0 or 1)`. In the main tree the same command is green only because `frontend/node_modules` happens to exist — a warm cache, not a passing suite. `_independent_total()` additionally shells out to `npx --yes knip@latest`, so it needs both Node and the network | This is exactly T17's BLOCKING 1, which the repair round was dispatched to fix and did not; T17 was later marked `verified` by a status-correction commit (`230b96d`) that reinstated it from a stale `blocked`, so nobody re-ran the CI case. Found again while measuring T21's base; T21's scope is a fourth checker, and widening it into the CI workflow would put the gate's own tests and the CI definition in one unreviewed diff | **AC1 and AC15 both.** AC15 (`CI green for the exact commit at HEAD`) cannot pass: the last CI run on this branch is `acc7b9a` (T18), and the five commits since — `e8b43dc`, `d3941db`, `3878ae7`, `094078a`, `230b96d` — are unpushed, so the breakage has never been seen by CI. Fix is one of: add a Node setup + `npm ci` step to the `test` job, or move the script-running tests to the `frontend-test` job's environment. **Not `skipif`** — B88 and T17's direction both record the manager declining that. Whoever takes it must re-run the fresh-clone reproduction above, not the main tree | M |
| B98 | **T21's dead-docs capability check cannot tell an advertisement from a removal note, and does not know the English word.** `scripts/deaddocs_check.py:293-302` matches a bare case-insensitive `\bteams\b` across `README.md` and `docs/`. Appending "Teams ingestion was removed; this app reads Outlook mail only." to `README.md` raises the count 12→13 (manager, verified); so does "Small teams of agents share one board" (reviewer, verified). The docstring states this reasoning as the grounds for keeping `.companion/*.md` out of capability scope but never states it for `README`/`docs/` | The check is still a net win at 8/14 true positives on B94 plus 2 B94 never cited, and the class is latent rather than live — it fires only once someone edits the docs | **B94's repair is the very next consumer**: whoever closes B94 must delete the word rather than narrate the removal, or the gate stays lit and the count cannot reach zero. Document it in the docstring at minimum. Found by the manager, widened and confirmed by T21's reviewer 2026-09-19. **Half fixed by T21 round 3 (`6217668`)**: the regex is now case-insensitive but rejects the plain-lowercase spelling, so "small teams of agents" is no longer flagged (manager-verified: 19, unchanged) while `Teams`, `TEAMS` and a future `OneDrive` all are. The removal-note half **stands** — "Teams ingestion was removed" appended to README still moves 19→20 (manager-verified 2026-09-19). Distinguishing an advertisement from a recorded removal needs negation detection the checker does not have and the docstring disclaims | S |
| B99 | **`_historic_source_kinds` matches any `*_KIND = "..."` assignment in any migration, not only sourcekind removals** (`scripts/deaddocs_check.py:281-286`). A future `EVIDENCE_KIND = "email"` would retire the word "email" and flag most of `README.md` | Today's derivation is correct and the manager falsified it: live `{manual, outlook_email}` + historic `teams_message` → retired `{teams}`, and re-adding the enum member drops the count 12→2. The defect is hypothetical until the next `*_KIND` constant lands | Restrict to constants whose value appears in a prior `sourcekind` listing, or require the name `REMOVED_SOURCE_KIND`. Cheap now, confusing later. Found by T21's reviewer 2026-09-19 | S |
| B100 | **The dead-docs path check does not scan fenced code blocks** — only backtick spans and markdown links (`scripts/deaddocs_check.py:114-117`). A README fence containing `cat docs/definitely-gone.md` is not flagged (reviewer, verified), though `-m` module commands inside fences *are*. **The README bug that motivated T21 (the broken `docker compose cp`, cc2a177) was itself inside a fence** | The task body's stated proof — a backticked dead path raises the total — holds, and the round trip 11→12→11 was measured. Widening to fences risks flagging every illustrative command in `docs/second-pc.md` | Means the gate would *not* have caught its own motivating example. Worth resolving before anyone claims T21 closed that class. Found by T21's reviewer 2026-09-19 | M |
| B101 | ~~A markdown link to a **live** file carrying an `#anchor` fragment is reported dead (`scripts/deaddocs_check.py:158-172`): the fragment is not stripped before the path is resolved. Appending `See [x](docs/second-pc.md#prerequisites).` to `README.md` moves the count 19→**20** although the file exists~~ **Fixed by T22**: `_strip_fragment` strips `#fragment` before resolution, pinned by `test_anchored_link_to_a_live_file_does_not_raise_the_count` / `test_anchored_link_to_a_dead_file_is_still_flagged` in `backend/tests/test_deaddocs_check.py`. Real-repo delta measured: `.companion/backlog.md:108`'s own `docs/second-pc.md#prerequisites` link stopped being flagged, moving the gate 22→21. **T22 round 2 correction (2026-09-19):** the `?query` half was never reached — `_looks_like_path` rejects every `?`-bearing token first — so it is not fixed and is now recorded as a gap in the checker's docstring and pinned by `test_query_string_link_is_a_documented_gap_not_a_finding` | Pre-existing at T21's base (18→19 there too), not introduced by any T21 round, ~~and latent in practice: the repo's only anchored link is `docs/second-pc.md:56`'s `../README.md#...`, which the `../` rule discards first, so no live finding comes from it today~~ **Correction (T22 round 2, 2026-09-19):** that was false when written. This row's own `docs/second-pc.md#prerequisites` was a second anchored link, and running the pre-fix checker over an extracted copy of `fd21b77` prints `dead doc path 'docs/second-pc.md#prerequisites' (.companion/backlog.md:108)` — a live finding, which is exactly what the first column's measured 22→21 delta says | It will fire the moment anyone adds a same-repo anchored link — and T22 is about to edit exactly these docs. Not listed in the docstring's accepted-false-positive set, so a reader has no warning. Fix: strip `#fragment` (and `?query`) before resolving, or document the class. Found by T21's round-3 reviewer, reproduced by the manager 2026-09-19 | S |
| B102 | ~~Two real-repo citations in `backend/tests/test_deaddocs_check.py` docstrings are stale. `test_line_number_suffix_is_stripped_before_the_path_is_resolved:225-227` says neutering `_strip_line_suffix` "takes this repository's own count from 1 to 134"; measured today it is **19 → 166**. `test_a_markdown_link_with_a_backticked_label_counts_its_path_once:391-395` cites "11 to 13" and "to 12"; on today's code, with the `seen` de-dup removed, the link form moves 19→**21** and the plain form 19→**20**~~ **Fixed by T22**: both docstrings restated against today's baseline (6, not 19) and dated 2026-09-19 — line-suffix neutering now cites 6→168, the de-dup removal now cites link 6→8 / plain 6→7, both re-measured, not copied forward | Both mechanisms are genuinely pinned — the fixtures redden under their mutations — so only the prose is wrong, and T21 round 3 was scoped to the two reviewer blockers plus four named advisories, not a docstring sweep. The round-3 worker measured both and correctly left them alone rather than widening its own diff | These numbers are the project's chosen evidence format for "this test is not vacuous", so a stale one quietly erodes the convention that made rounds 1-2's vacuity visible at all. The second is worse than stale: it describes a pre-fix bug against an 11 baseline, so it needs rewording, not renumbering. Fix: restate both against today's baseline, or date each measurement. Found by T21's round-3 worker, confirmed by its reviewer 2026-09-19 | S |
| B103 | ~~`scripts/deaddocs_check.py:10-11` says `.companion/harness-notes.md` is excluded because "its own **first line** says it documents 'the project-companion repo'". The first line is `# Harness notes — misbehaviour of the loop itself`; the claim lives on line 3, and it says the entries are *to be carried to* that repo~~ **Fixed by T22**: reworded to "its own header says it is 'for the owner to carry to the project-companion repo'", matching `.companion/harness-notes.md:3` verbatim | Pre-dates T21's base (`9d6e1a7`) and the exclusion it justifies is correct and tested (`test_harness_notes_file_is_excluded_from_the_path_scan` reddens when the file is put back in scope, moving the repo 19→20) | Same class as the defect that blocked T21 three rounds running — a docstring sentence that does not survive `head -3` of the file it describes. Cheap to correct and worth correcting while the file is being edited anyway. Fix: "its own header says its entries are to be carried to the project-companion repo". Found by T21's round-3 reviewer 2026-09-19 | S |
| B104 | **Installed-agent metadata still advertises Teams ingestion that T19 removed, while `README.md` and `docs/` now say mail-only.** `skills/pending-work/SKILL.md:3,14,15` (the skill `description` an agent matches on, plus two handling rules) and `.codex-plugin/plugin.json:9,14` (a `teams` keyword and a `longDescription`) are the last 5 of B94's 14 references still standing — verified by `grep -ni teams` on both files 2026-09-19 | Outside the dead-docs checker's declared doc scope, which is `README.md`, `docs/` and `.companion/*.md`; its "Known gaps" block names these two files explicitly, so the gate cannot see them and T22's done-when cannot be made to care. T22's scope is the gate's findings, not every stale string in the repo | The audience is an installed agent, not a human reader: a skill whose `description` promises Teams material gets matched for a request the connector can no longer serve. T22 widened the gap by making `README.md`/`docs/` mail-only, so the two halves of the repo now contradict each other outright. Fix: delete the Teams clause from all five lines. Found by T22's round-1 reviewer, line numbers verified by T22 round 2 2026-09-19 | S |
| B105 | **`docs/architecture.md:19` still says the Graph connector "calls `/me` only"** while `backend/app/integrations/graph.py:41,47` call both `/me?$select=id,userPrincipalName,mail` and `/me/mailFolders/inbox/messages` | Defensible if "`/me`" names the namespace rather than one endpoint, and pre-existing — base said it too. T22's repair rewrote the scope half of this exact clause and deliberately left the `/me` half, flagging it rather than widening its own diff | A reader checking the boundary claim against the code meets two endpoints where the sentence promises one. Fix: "calls only under `/me`". Found by T22's repair worker, confirmed by its reviewer 2026-09-19 | S |
| B106 | **B102's own row in this file cites a third, different number.** Its "Fixed by T22" text says the line-suffix docstring "now cites 6→168"; the test at `backend/tests/test_deaddocs_check.py:225-227` as shipped cites 169, and the live measurement is 171. The row contradicts the file it describes | Cosmetic in isolation, but B102 exists *because* these citations went stale, so a wrong number inside B102's own closure is the defect describing itself | Fix together with B107 — whatever the test finally cites, this row must repeat it. Found by T22's second reviewer 2026-09-19 | S |
| B107 | **The real-repo casualty citations in `backend/tests/test_deaddocs_check.py` invalidate themselves.** Every one (line-suffix 6→169, de-dup 6→8/6→7, the query-string gap 6 vs 7) shifts whenever any in-scope doc gains a line-cited path. T22's own last commit moved the line-suffix figure from a correctly-measured 164/169 to 166/171 before the round ended, and the manager's T22 progress entry moved it again | **This is the third round the class has bitten** — B102 raised it, T22's attempt restated it wrongly, T22's repair restated it correctly and then broke it again two commits later. Dating the measurement is not enough: the sentence still reads as a live claim | Pin each citation to the commit it was measured at, e.g. "measured at `34dbedf`: the docs checker alone goes 1 to 166, the whole gate 6 to 171" — a sha-pinned measurement stays verifiable forever instead of drifting. Or drop the gate-total figure and let the fixture assertion be the sole pin. Found by T22's second reviewer 2026-09-19 | M |
| B108 | **A process whose command line is relative escapes the gate, even with its cwd inside the repository.** `scripts/leave-no-trace.sh:178-179` matches `/proc/<pid>/cmdline` against the main worktree's path only. **Reproduced by the manager 2026-09-20:** `( cd /home/eimi/projects/magic-tower/frontend && exec sleep 25 ) &` is alive with `readlink /proc/<pid>/cwd` = `.../magic-tower/frontend`, and the gate still prints `repo-pointing processes: 0` | T01's plan body says "running processes whose command line points inside this repository", so the script is faithful to its own spec, and widening to `/proc/<pid>/cwd` would count every editor, shell or terminal a human has parked in the repo — the gate would go red for things nobody left behind | The retro's motivating example is a vite dev server left alive for hours; whether it is caught depends on how it was launched (`node .../node_modules/.bin/vite` is caught, a relative-argv child of a repo-cwd shell is not), so the category cannot be said to close that class. Fix: add cwd as a *second* signal with an explicit exclusion for interactive shells, or record the limit in the header. Found by T01's reviewer, reproduced by the manager 2026-09-20 | S |
| B109 | **`/tmp` leftovers that do not *begin* with a task ID are invisible to the gate.** `scripts/leave-no-trace.sh:141` anchors the pattern at `^`, so `/tmp/probe-t13.json` — a real 7854-byte leftover dated 2026-09-18, confirmed present by the manager 2026-09-20 — is never counted | The anchor is what stops the 338 nanoid-shaped entries in this machine's `/tmp` from being counted as this project's trace (T01's first blocking finding); loosening it to an unanchored search would bring them straight back | The gate under-reports exactly the class it exists to measure, and the 224-files-in-`/tmp` retro is the reason the category exists. Fix: match a task ID anywhere in the basename but require the terminator on both sides (`(^|[._-])(ac|t)[0-9]+([._-]|$)`), and re-measure the nanoid collision count before adopting it. Found by T01's repair worker, confirmed by the manager 2026-09-20 | S |
| B110 | **`test_leave_no_trace_script_exists_and_is_runnable` (`backend/tests/test_leave_no_trace_script.py:57-58`) asserts only `SCRIPT.is_file()`** — a script with a syntax error passes it, so the name over-promises | Redundant rather than vacuous: the five sibling tests all execute the script and parse its output, so a broken script reddens the suite regardless | Cosmetic, but this repo has blocked two tasks over test prose that does not survive the code it describes. Fix: rename to `…_exists`, or assert `bash -n` exits 0. Found by T01's reviewer 2026-09-20 | S |
| B111 | **A worktree reached through a symlinked path may count itself as stray.** `scripts/leave-no-trace.sh:80` resolves `repo_root` with `pwd` (symlinks collapsed) while `git worktree list --porcelain` prints git's recorded path; the `= "$repo_root"` self-exclusion then fails | Not reproducible on this machine — no symlinked worktree exists — and the `first=1` skip already protects the main-tree case, which is where the manager runs the gate | Would make the gate red for the very worktree running it, which is the failure mode the self-exclusion was written to prevent. Fix: compare `git rev-parse --git-common-dir`-derived paths, or resolve both sides with `realpath`. Found by T01's reviewer 2026-09-20 | S |
| B112 | **AC4 cannot be self-served in this environment: the manager is denied permission to delete or move anything under `/tmp`.** Three formulations were refused by the permission classifier on 2026-09-20 (`rm -f /tmp/ac13.json /tmp/t2.json`, `mv … .companion/runs/<run>/tmp/`, and a bare `rm /tmp/ac13.json`), with reasons *Irreversible Local Destruction* and *Interfere With Workloads* | Routing the deletion through a subagent would be laundering the same denial, and the harness's own guidance is to surface it to the owner rather than work around it | **This is what blocks T01**, and it will block AC4 for every later task too: the gate's `/tmp` category can be measured but never cleaned by the agent that measures it. Today the whole remainder is one file — `/tmp/t2.json`, a stale vitest report from a previous contract's T2, 7878 bytes, dated 2026-09-18. Fix: the owner runs `rm /tmp/t2.json /tmp/probe-t13.json`, or grants a `Bash(rm:/tmp/*)`-shaped permission rule. Found by the manager 2026-09-20 | S |
| B113 | **Nothing asserts the four `JIRA_*` rows in `.env.example`.** T03's vacuity probe reported `.env.example`=**VACUOUS** — reverting it to base leaves all 14 `jira_configuration` tests green, and the T03 reviewer confirmed independently that deleting all four rows leaves `scripts/deaddocs_check.py` output byte-identical at 4 findings | The task mandated the rows and they are correct and value-free; no existing gate covers `.env.example` for the Graph settings either, so this is a pre-existing hole T03 merely inherits | A setting can be added to `config.py` and silently never documented for the owner, which is the one place the env-var names are discoverable. Fix: assert `.env.example` contains the upper-cased name of every entry in `_BLANKABLE_OPTIONAL_FIELDS`. Found by the manager's probe and the T03 reviewer 2026-09-20 | S |
| B114 | **`Settings.jira_configuration_errors()` (`backend/app/config.py:118`) has no non-test caller**, and `jira_management_project_key` (`config.py:53`) is read nowhere. `uv run --with vulture vulture app --min-confidence 60` reports both; the sealed gate runs at `--min-confidence 80`, so neither counts today (gate **9** vs baseline **10**) | Exactly what T03's body mandates — the method is specified by the task and T04-T06 are its intended callers. Blocking on unreachability at T03 would forbid writing the settings layer before the client that consumes it | Becomes permanently dead code, and a real AC5 risk if the confidence threshold ever drops, should no T04-T06 caller land. Whoever closes T06 should confirm the fail-closed path actually calls it rather than re-deriving the check. Found by the T03 reviewer 2026-09-20 | S |
| B115 | **`jira_configuration_errors()` checks presence but not shape.** Reviewer-verified: `Settings(jira_site_url="example.atlassian.net", jira_account_email="not-an-email", jira_api_token="t").jira_configuration_errors()` returns `[]` — a schemeless host and a non-email both pass the fail-closed check | AC12 is scoped to "missing or blank", which is exactly what the method implements. Graph's `graph_identifiers_are_not_urls` validates the *opposite* direction (IDs must not look like URLs), so there was nothing to mirror for a field that is meant to be a URL | The misconfiguration surfaces as a T04 client error or a 401 against the wrong host rather than as a named env var, which is the experience AC12 exists to prevent. Fix belongs with T04, which owns `https://<site>.atlassian.net` construction. Found by the T03 reviewer 2026-09-20 | S |
| B116 | **The "asserts an absence" false-positive class lost its heading.** T02 removed the instance from `scripts/deaddocs_check.py`'s "Known false positives" block (the only instance, `.companion/progress.md:92`, was resolved by creating the file) and left the class itself documented only mid-sentence inside the backticks-convention bullet | The class is still stated in present tense there ("This checker does no negation detection"), so nothing false ships; moving it is a discoverability edit, not a correctness one, and T02 was scoped to the baseline | An author who writes another "there is no `some/path`" line into an in-scope doc will scan the "Known false positives" and "Known gaps" headings, not the convention bullet, and will lose a finding to a sentence that is doing its job. Fix: one line under "Known gaps" naming the class. Found by the T02 reviewer 2026-09-20 | S |
| B117 | **The Jira client accepts the scoped-token host it was written to avoid.** Manager-reproduced 2026-09-20: `JiraClient("https://api.atlassian.com/ex/jira/abc", "e@x", "t", transport=...).myself()` constructs and requests `https://api.atlassian.com/rest/api/3/myself` — `_validated_base_url` in `backend/app/integrations/jira.py` checks the URL's *shape* (https + netloc) and strips the path, never its identity | T04 asked for construction against `https://<site>.atlassian.net`, which it does; rejecting a host by name is a policy the task never specified, and the requirement is met at the documentation level in `.env.example` and the settings docstrings | A pasted scoped-token URL fails as a 401 at request time rather than as a named env var, which is the experience AC12 exists to prevent. The docstrings no longer claim otherwise (`ec462db`). Fix: reject a netloc that is not `<site>.atlassian.net`, or accept and say so. Found by the T04 repair reviewer 2026-09-20 | S |
| B118 | **The dead-code gate's vulture count is suppressed by one unrelated symbol, so the number 3 is fragile.** Manager-verified 2026-09-20: with `backend/app/integrations/jira.py` present, `uv run --with vulture vulture app tests --min-confidence 80` reports **0**; with it removed it reports **3**, all `unused variable 'cls'` in `backend/app/config.py` at lines 65, 84 and 95 (pydantic validator false positives). `JiraClient.from_settings` genuinely reads `cls`, and vulture's unused-variable check is whole-tree name matching, not scope-aware | Nothing is wrong with the code on either side: the classmethod is real and the config findings were always false positives. The gate moved 7 to 3 for this reason plus one expected docs finding, and AC5 only requires at-or-below baseline | Deleting or renaming `from_settings` would silently add 3 to the gate and look like a regression in whatever task did it, and conversely any future `cls`-reading classmethod hides them again. The recorded baseline of 7 in `.companion/deadcode.baseline` is a ceiling, not a measurement of this tree. Fix: add `cls` to a vulture whitelist so the count stops depending on an unrelated file. Found by the T04 worker, reproduced by the manager 2026-09-20 | S |
| B119 | **`jira_account_email` shape is validated nowhere**, which is the half of B115 T04 did not take. T04 closed the site-URL half: `_validated_base_url` rejects schemeless and non-https URLs at construction, tested three ways. A non-email string still passes both `jira_configuration_errors()` and the client and is base64-encoded straight into the Basic auth header | The T04 worker declined to invent an email regex unprompted and flagged it rather than skipping it silently; the reviewer agreed it was not blocking, since the task named the host construction and not the email | Surfaces as a 401 against a correct host rather than as a named env var. Fix belongs wherever the first real request is made — T06's sync handler, or the setup command T12 ships. Found by the T04 worker and reviewer 2026-09-20 | S |
| B120 | **The bounded-pagination test aborts the whole pytest session instead of failing.** `backend/tests/test_jira_query.py:153-165` asserts `call_count == JIRA_SEARCH_MAX_PAGES`, but the mutation it exists to catch — replacing `for _ in range(JIRA_SEARCH_MAX_PAGES)` with `while True` — never reaches that assertion. Reviewer-measured 2026-09-20: pytest-timeout (`backend/pyproject.toml:40-41`, `timeout = 30`, `timeout_method = "thread"`) fires at 33.4s, rc=1, and its `os._exit` kills the session with no per-test summary line | The test is not vacuous — it pins the bound's *value*, and changing `JIRA_SEARCH_MAX_PAGES` to a literal other than the constant reddens it normally. The suite does go red, so the gate is not blind; the cost is diagnosis, not correctness | A removed bound reports as "the suite died at 33 seconds" rather than "this test failed", which is the least legible failure mode in the suite and burns 30s of every CI run that hits it. Fix: make the stub transport raise once `call_count > JIRA_SEARCH_MAX_PAGES`, so the bound is asserted rather than merely survived. Found by the T05 reviewer 2026-09-20 | S |
| B121 | **Hitting the page cap truncates silently.** `backend/app/integrations/jira.py:143-156`: a server returning 20 full pages makes `search_participation_issues` return 2000 issues and stop, with no raise, no log and no flag on the return value — the caller cannot distinguish "that was everything" from "there was more" | Deliberate and correct for T05's scope: the task said the query must be **bounded**, and 20 x 100 is far above what one account's 15-minute participation window holds. Signalling truncation is a return-shape decision that belongs with the consumer, not the client | The queue would silently under-report during a bulk edit or a migration — exactly the burst that overruns the window. T06 owns the sync handler and is the first caller that could act on the signal. Fix: return a `(issues, truncated)` pair or raise on cap-exhaustion, and decide there whether a truncated poll should re-poll immediately. Found by the T05 reviewer 2026-09-20 | S |
| B122 | **`extract_adf_plain_text` (`backend/app/integrations/jira.py:158-180`) has no production caller and no code path can reach it**, because `description` is deliberately absent from `JIRA_SEARCH_FIELDS` (`jira.py:49`). Vulture at the gate's `--min-confidence 80` cannot see it, so nothing enforces that T06 wires it up (gate **3** vs baseline 7, unchanged by this diff) | T05's task body explicitly names ADF plain-text extraction, and the worker kept the fields list to exactly AC8's seven names rather than widening scope on its own. The function is independently tested (nested-node and `None` cases), so it is specified and covered, just not reachable | Same shape as the defect that cost T04 its repair round — a method with no caller anywhere (`.companion/progress.md:977`) — the difference being that this one is mandated by its task. It goes permanently dead if T06 never fetches descriptions. Whoever closes T06 must either request `description` in the fields list and call this, or record why descriptions are not fetched. Found by the T05 reviewer 2026-09-20 | S |
| B123 | **Page size and the cap's value are unpinned by any test.** Reviewer-measured 2026-09-20: deleting the `"maxResults": str(JIRA_SEARCH_PAGE_SIZE)` parameter (`backend/app/integrations/jira.py:143`) leaves 18 passed, and changing `JIRA_SEARCH_MAX_PAGES` from 20 to 5 also leaves 18 passed, because the bound test compares against the constant rather than a literal | Neither is an AC8 clause — AC8 requires the JQL, the bound's existence, cursor paging and explicit fields, all of which are pinned. A test asserting a tuning constant equals itself would be the vacuity this contract keeps hunting | Dropping `maxResults` silently falls back to Jira's server default, changing the request shape and the number of round trips with no test noticing. Fix: assert the literal `maxResults=100` on the first request URL, the way the endpoint path and `fields` are already asserted. Found by the T05 reviewer 2026-09-20 | S |
| B124 | **A malformed `{"issues": null}` response raises `TypeError`, not `JiraError`.** `backend/app/integrations/jira.py:152` uses `response.get("issues", [])`, which returns `None` rather than the default when the key is present and null, so `issues.extend(None)` raises out of the connector's own exception hierarchy | Jira does not send a null `issues` array, and every other malformed-response path is already funnelled through `default_transport`'s catch-all into `JiraError`. T05 was scoped to the query, not to response-shape hardening | A caller that catches `JiraError` around a sync — which is what T06 will write — would not catch this, so one malformed page takes down the poll instead of failing the source. Fix: `response.get("issues") or []`. Found by the T05 reviewer 2026-09-20 | S |
| — | **T06 fixed B124** (`backend/app/integrations/jira.py:152`, `or []` in place of the `.get(..., [])` default) with a new falsifying test, `test_jira_query_treats_a_null_issues_array_as_empty_instead_of_erroring` — confirmed red without the fix (`TypeError: 'NoneType' object is not iterable`), green with it | — | Closes B124 | — |
| — | **T06 declined B121 (page-cap truncation signalling).** The sync handler (`backend/app/services/jira_sync.py`) calls `search_participation_issues()` as-is and does not detect or flag a full 20-page response. Changing the client's return shape to signal truncation would touch `search_participation_issues`'s tested return type (a bare `list`), which every test in `test_jira_query.py` — pinned, not to be edited — asserts against directly | Left for whoever next touches the client's public return shape, or for a task that owns cross-cutting truncation handling; T06 was scoped to "sync handler", not "harden the client's contract" | B121 stands, now explicitly declined rather than silently dropped | S |
| — | **T06 declined B122 (wire up `extract_adf_plain_text`).** `JIRA_SEARCH_FIELDS` (`backend/app/integrations/jira.py:49`) is pinned to its exact seven-name tuple by `test_jira_query_requests_fields_explicitly_instead_of_the_id_only_default`; adding `"description"` would require editing that existing, passing test. No loop rule forbids that outright; the honest reason is scope — T05 pinned the list to exactly AC8's seven names, and widening it is a decision T06's body does not carry. `extract_adf_plain_text` stays uncalled | Deferred to whichever task first needs Jira descriptions in the queue (not named in `.companion/plan.md` T06-T12) | B122 stands, now explicitly declined rather than silently dropped; the function remains dead but under vulture's `--min-confidence 80` radar, so the gate does not see it either | S |
| — | **T06 declined B119 (validate `jira_account_email` shape).** The sync handler only consumes `JiraClient.from_settings(settings)`, which already fails closed on a *missing* email via `jira_configuration_errors()`; adding a format check there would mean writing an email regex into either `Settings` or `JiraClient`, neither of which "Jira sync handler" (T06's scope) asked for | B119 itself named T06's sync handler *or* T12's setup command as candidate fix sites; T12 (the setup doc and its verification command, not yet on disk) is the better fit, since it is the one place a malformed email is caught before it reaches a live request | A non-email string still base64-encodes into the Basic auth header and surfaces as a 401, not a named env var, until T12 lands | S |
| B125 | **The dedup's first-wins policy is unfalsified.** Manager-reproduced 2026-09-20: `deduped.setdefault(row["external_id"], row)` → `deduped[row["external_id"]] = row` (`backend/app/services/jira_sync.py:70`) leaves the T06 gate at **13 passed, exit 0**. `test_jira_sync_keeps_one_signal_for_an_issue_repeated_across_two_pages` feeds two *identical* rows, so first-wins and last-wins are indistinguishable | The test was added in T06's repair round to prove the dedup block is reachable at all, which it does; which copy survives is a second question the round-1 finding did not name | If the two copies ever differ — the duplicate arriving because the window was re-evaluated mid-walk is exactly the case where the second copy is the fresher one — first-wins stores the **stale** `observed_at`, the indexed column the queue sorts on. Fix: feed `[[ISSUE], [ISSUE with a later updated]]` and assert which `observed_at` is stored, whichever policy is intended. Found by the T06 repair reviewer, reproduced by the manager 2026-09-20 | S |
| B126 | **The production client-construction fallback is never executed by the suite.** `jira = jira_client or JiraClient.from_settings(settings)` (`backend/app/services/jira_sync.py:89`) → `jira = jira_client` leaves the T06 gate at **13 passed**: every configured-path test injects `jira_client`, so the branch that builds the real client from settings is only reached in production | Injecting the stub through `jira_client` is the no-network seam the quality bar demands, and T06's tests are right to use it; exercising the fallback needs a monkeypatch, which is a different testing style than the rest of the file | A regression here fails loudly (`AttributeError` on `None`) rather than silently, so it is not a correctness risk today — but the one line that turns settings into a live client is unproven, and T12 proves the connector against real Jira, where it is the only path. Fix: one test monkeypatching `app.services.jira_sync.JiraClient.from_settings` to return the stub, calling `sync(settings, kind="jira", db=session)` with no `jira_client`. Found by the T06 repair reviewer 2026-09-20 | S |
| B127 | **Five of T05's seven explicitly-requested fields are dropped on the floor.** `_normalize_issue` (`backend/app/services/jira_sync.py`) emits only `external_id`, `source_kind`, `title`, `source_url`, `observed_at`; `status`, `priority`, `assignee`, `reporter` and `project` are fetched over the wire and discarded. Round-1 reviewer measured end to end: `Source.excerpt=None`, `SourceSignalContext=(None,None,None,None)`, `WorkItem.status=pending`, `priority=medium` for an issue reading "In Progress"/"Highest" | Not a T06 breach — T06's body and AC6 ask only that an issue "becomes a `Source`", and the schema forbids new `sources` columns (`0001_initial_schema.py:31`, `test_migrations.py:247`). Both reviewers and the manager agreed it is outside this task | **The contract's Result explicitly promises Jira issues appear "with real status, priority and assignee", and no remaining task names persisting status or priority.** T07 owns `assignee` (for promotion) and T09 owns `project` (for visibility), so those two get carriers; status and priority are currently **unowned** by T07-T12. Also leaves `jira.py:48`'s comment ("everything the queue needs to render a Jira issue must be named here") describing five constants with no consumer. Fix: decide a carrier (`SourceSignalContext` or configuration) in T07, or add a task. Found by the T06 round-1 reviewer, confirmed by the manager 2026-09-20 | M |
| B128 | **Four more lines of the sync handler survive mutation**, all mirroring gaps in `_sync_graph`'s own coverage rather than T06 regressions: `"synced_at": None` (13 passed), `"new_work_items": created` (13 passed — structurally unfalsifiable until T07 gives promotion something to reject), and dropping either `if db is not None else 0` guard (13 passed) | Baseline-shaped: the Graph handler these mirror has the same holes, so closing them in the Jira handler alone would be asymmetric, and none is a behaviour T06's body or AC6 names | The envelope's `synced_at` and `new_work_items` are what `POST /api/sync` returns to the GUI (T11) and the CLI (T10), so a wrong value there is user-visible and untested on both handlers. The T06 test module docstring now names these four explicitly rather than claiming every rule is falsified. Fix: pin the envelope's values, for both handlers, in whichever task next touches `sync()`. Found by the T06 repair reviewer 2026-09-20 | S |
| B129 | **AC7 names watcher, voter, reporter and creator, and no test names any of them.** `_issue()` (`backend/tests/test_jira_promotion.py:60`) accepts `**fields` but every one of the twelve tests passes only `assignee`; the four involvements AC7 lists are covered only by implication, through "assignee is somebody else" and "assignee is absent" | The rule is keyed on assignment alone, so a reporter fixture exercises the same branch as an unassigned one — there is no second code path to cover, and the reviewer reproduced the behaviour directly (owner as `reporter`+`creator`, assignee a colleague or `None` → 2 stored, 0 promoted). Adding a test is not a manager repair under the loop's textual exception, and the gate was green | The criterion's own vocabulary is absent from the suite, so a future change that special-cased reporter involvement would not redden anything, and a reader auditing AC7 finds nothing to read. Fix: one fixture with `reporter`/`creator` set to the owner, asserting the `Source` is stored and `new_work_items == 0`. Found by the T07 reviewer, reproduced 2026-09-20 | S |
| B130 | **`GET /rest/api/3/myself` returning a 200 whose body is not a JSON object aborts the sync.** Manager-reproduced 2026-09-20: a transport returning `[]` makes `client.myself().get("accountId")` (`backend/app/services/jira_sync.py`) raise `AttributeError`, which is not a `JiraError`, so `_owner_account_id`'s `except` does not catch it; the issues are already committed by `persist_signals`, and `POST /api/sync` (`backend/app/api/sync.py:24`) catches only `SyncError`, so it would 500 | Promotion still fails closed — nothing is promoted on that path — and the same shape exists at base in the client's own `response.get("issues")` handling (B124), so it is baseline-adjacent rather than a T07 regression. The two docstrings that overstated it were narrowed by the manager instead | A malformed or proxied `/myself` response turns a partially-successful sync into an unhandled 500 rather than a sync that stored its issues and promoted none. Fix: `isinstance(body, dict)` in `_owner_account_id`, or raise `JiraError` on a non-object body in the client, which would close B124 at the same time. Found by the T07 reviewer, reproduced by the manager 2026-09-20 | S |
| B131 | **`POST /api/sources/{id}/promote` is a third door that ignores the issue link.** `promote_source` (`backend/app/services/promotion.py:243`) creates a work item without consulting or writing a `work_item_issue_keys` row, so an owner manually promoting a Jira mail `Source` whose issue the API already queued gets a second item. Reviewer-reproduced 2026-09-20 (repro section F) | T08's body scopes the match to the two automatic arrivals — notification mail and the participation query. Manual promotion is the owner deliberately asking for an item, and whether it should silently merge into an existing one is a product question T08 never asked | The duplicate AC9 exists to prevent, on a path the owner drives by hand. Fix is to route `promote_source` through `issue_key`/`work_item_for_issue` too | S |
| B132 | **The backfill cannot dedupe.** `_signal_from_source` rebuilds a Jira signal from stored `sources` rows with no `issue_key`, so `issue_key()` returns `None` and no match is attempted. Same root cause T07 recorded for its promotion rule: the key rides on the normalized signal, not a column | Jira rows are declined by the backfill anyway (T07), so no duplicate is created today; a backfilled *mail* row still gets its key extracted normally because its sender comes from `SourceSignalContext` | Any future task that lets the backfill re-decide Jira sources — it would create unlinked items. Blocks nothing today | S |
| B133 | **Two Jira issues sharing one key but different numeric ids do not replace each other.** `merge_into` (`backend/app/services/jira_dedupe.py`) adopts an item's identity only when the existing item is *not* already Jira, so a deleted-and-reused key leaves the first issue's identity in place. Untested | The T08 worker declined to pin a behaviour it had not seen Jira produce, and the manager agreed: a test asserting invented semantics is worse than none | Nothing observed. Would matter only on a site that deletes and recreates issue keys | S |
| B134 | **A work item promoted before revision `0009` is never linked to its issue.** `promote_signals` skips an existing `source_external_id` (`backend/app/services/promotion.py:308`) *before* `issue_key`/`link_issue` run, so re-seeing the same notification mail never creates the missing link row; the API arrival then creates a second work item. Reviewer-reproduced and manager-reproduced 2026-09-20 (repro section C): re-seen mail `created = 0`, links `0`, then `api created = 1` and two rows for one issue | The migration deliberately guesses nothing, and backfilling links for pre-existing items is new scope T08 never asked for. T08's repair round corrects the migration docstring to say such an item is *never* matched rather than 'not until it is seen again' | One duplicate per Jira work item that was already in the queue when `0009` shipped — a bounded, one-off population, empty on a fresh database. Fix is a data migration or a link-before-skip branch | S |
| B135 | **Every Jira test opens `Session(engine)` (autoflush on) while production uses `SessionLocal` (`backend/app/database.py:18`, `autoflush=False`).** T08's repair adds one test on the production factory, but the rest of the suite keeps the divergence, so any other pending-session assumption is equally invisible | Converting the suite's session construction is a cross-cutting change to tests no task in this contract owns, and doing it inside T08 would rewrite tests belonging to T03-T07 | The exact class of defect that made T08's repair round necessary: an `IntegrityError` on the common path that all 13 new tests passed straight through. Any future code that reads back a row it has only `db.add`ed | M |
| B136 | **A Jira issue with no `key` would fold every other keyless issue into one work item, and the guard that prevents it has no test.** Manager-reproduced 2026-09-20: `return str(key) if key else None` → `return str(key)` (`backend/app/services/jira_dedupe.py:88`) **survives both** `-k jira_dedupe` (15 passed) and the full suite (359 passed). The literal string `"None"` then becomes a shared issue key | The shipped code is correct — the guard is present and right; only its test is missing. `_normalize_issue` already guards `if key` when building `source_url`, so the codebase treats a keyless issue as reachable, but Jira does not return one in practice | The quality bar's "each rule falsifiable individually". Fix: assert `issue_key(_normalize_issue({"id": "1", "fields": {}}, SITE_URL)) is None`. Found by the T08 repair reviewer, reproduced by the manager 2026-09-20 | S |
| B137 | **Four more extractor rules survive mutation on both the gate and the full suite**, each one a rule whose own comment claims deliberate narrowness (`backend/app/services/jira_dedupe.py:45,51,55,130`): `JIRA_MAIL_SENDER_MARKERS = ("jira",)` — every fixture sender contains *both* markers, so `atlassian` is untested; `_ISSUE_KEY` `+`→`*`, admitting the one-letter project keys the comment excludes; `_SUBJECT_KEY` without `\b`, matching `OPS-42` inside `OPS-42x`; `merge_into` without `and item.source_kind != SourceKind.jira` | All four are coverage gaps rather than defects — the shipped behaviour is the documented one in every case. The repair round was scoped to three blocking findings plus the precedence gap, and widening it further would have risked the one round the loop allows | A future narrowing or widening of the key grammar, or of which senders count as Jira, would redden nothing. Fix: one extractor case per rule. Found by the T08 repair reviewer 2026-09-20 | S |
| B138 | **`_record_considered`'s pending-row blindness under `autoflush=False`.** With T08's `db.flush()` in `work_item_for_issue`, a batch of `[rejected, rejected-with-the-same-external_id, jira-keyed]` surfaces as a raw `sqlalchemy.exc.IntegrityError: UNIQUE constraint failed: source_promotions.source_id` (a 500) where it previously surfaced as `HTTPException 409` | Pre-existing, not introduced: the reviewer confirmed the identical batch *without* any Jira signal raises the same raw `IntegrityError` at the final commit today, and no real caller produces a duplicated `external_id` within one batch. The flush moved where it surfaces, not whether it happens | Nothing today. Fix: have `_record_considered` (`backend/app/services/promotion.py:225`) check the pending session as well as the flushed rows, or de-duplicate `external_id` within a batch. Found by the T08 repair reviewer 2026-09-20 | S |
| B139 | **Two prose overstatements the repair's own rewording left**, both narrow rather than false: `backend/app/services/jira_dedupe.py:100` says a missing flush makes "the **final** commit" fail, which holds only for a two-signal batch — with `[OPS-1, OPS-1, OPS-2]` the failure lands earlier, on the third signal's `create_work_item`, as `HTTPException 409`. And `backend/tests/test_jira_dedupe.py:12-14` says the replacement rewrites four fields "and nothing else", omitting the appended `WorkEvidence` rows and the `updated_at` bump from `onupdate` | Both are accurate about the case they describe and neither claims a mechanism the code lacks — the defect class that blocked round 1. The owner-field half was verified true: `priority`, `summary`, `assigned_agent` and `due_at` all survive a real merge. Not worth spending a manager edit on after the round closed | Readability only. Fix: "a later commit", and "no other column the owner sets". Found by the T08 repair reviewer 2026-09-20 | S |
| B140 | **`merge_into`'s `else None` is unreachable** (`backend/app/services/jira_dedupe.py:130`): a Jira payload without `source_url` also has no `key`, so `issue_key` is `None` and `merge_into` is never entered. Below vulture's `--min-confidence 80` threshold, so the gate cannot see it | The T08 repair worker checked reachability, kept the guard deliberately — removing it would make the line `str(payload.source_url)` and yield the string `"None"` if a future caller ever did reach it — and said so rather than deleting it silently. The guard matches the idiom `create_work_item` already uses | Nothing. Recorded so the next reader does not rediscover it as a finding. Found by the T08 round-1 reviewer, re-confirmed in the repair round 2026-09-20 | S |
| B141 | **A management project larger than 2000 issues loses its tail silently.** `search_project_issues` walks at most `JIRA_SEARCH_MAX_PAGES * JIRA_SEARCH_PAGE_SIZE` pages (measured by the manager: a stub that always returns `nextPageToken` gives 20 pages, 2000 issues, no raise), so past that only the most recently updated 2000 are visible and nothing on the result says so | T09's round-1 finding offered "say so in the docstring" or "make truncation observable"; the repair took the prose option and disclosed it. The cap's own justification in the code is about a 15-minute participation window and does not transfer to a whole-project query | Management visibility on any project over 2000 issues — the owner sees a recent-issues view believing it is the project. Fix: surface a truncation marker on the envelope, or page the project fetch on an `updated` watermark. Found by the T09 round-1 reviewer, measured by the manager 2026-09-20 | M |
| B142 | **The project query carries no `updated >=` clause**, so every sync re-walks the whole visible project (up to 20 requests on top of the participation window) and re-normalises every issue; a 429 anywhere in those pages raises `JiraRateLimitError` out of `_fetch_issue_signals` and fails the entire sync, including the participation window that has nothing to do with management visibility | T09 did not ask for incrementality, and `persist_signals` is idempotent, so the cost is requests rather than rows | Poll frequency, and the actionable sync's reliability — one optional setting becomes a new single point of failure for it. Fix: bound the project fetch on an `updated` watermark, or catch its errors separately from the participation fetch. Found by the T09 round-1 reviewer 2026-09-20 | M |
| B143 | **The two "never promotes" tests assert only absence** (`new_work_items == 0`, no `WorkItem`) and both stay green when the project fetch is deleted entirely — verified by the manager's M1 mutation, which reddens 2 of 9 tests and neither of these | Storage is covered separately by `..._stores_a_project_issue_as_a_browsable_source`, so the set is adequate today | An absence assertion an empty fetch satisfies degrades to a tautology the moment the fetch regresses. Fix: add `new_sources == 1` or a `Source` lookup to both. Found by the T09 round-1 reviewer, reproduced by the manager 2026-09-20 | S |
| B144 | **`.env.example`'s `JIRA_MANAGEMENT_PROJECT_KEY` comment says "Its issues are never promoted into the actionable queue"**, which T09 makes false for one input: an issue in that project assigned to the owner is promoted, by design and by the task's own requirement | The line predates T09's diff (identical at `b269591`) and AC10 uses the same loose phrasing | It is the only user-facing documentation of the feature and it contradicts the one exception the feature preserves. Fix: one clause, "unless it is assigned to you". Found by the T09 round-1 reviewer 2026-09-20 | S |
| B145 | **Two inert lines in `_fetch_issue_signals`**: `issues = list(client.search_participation_issues())` is a defensive copy against an aliasing hazard that cannot occur (`_search` always returns a fresh list), and `project_key: str | None = None` is an unreachable default — `_sync_jira` is the only caller and always passes it. Both survive mutation (manager and round-1 reviewer) | Neither changes behaviour | Readability only: the copy implies a hazard that does not exist, and an optional-with-default private helper invites a future caller to silently skip the project fetch. Found by the T09 round-1 reviewer 2026-09-20 | S |
| B146 | **`POST /api/sources/{source_id}/promote` (`backend/app/api/routes.py:106`) can promote a project-fetched Jira source by hand**, so AC10's "never promoted" holds for the sync path only, while three T09 docstrings say it unqualified | Manual promotion by the owner is the browse-only feature's intended follow-up, not flooding — the same judgement T08 recorded for `promote_source` as a third un-deduped door (B131) | Nothing today. Fix: add "by the sync" to the three sentences. Found by the T09 round-2 reviewer 2026-09-20 | S |
| B147 | **The escaper passes control characters through unescaped.** `search_project_issues` escapes `\` and `"` but not a raw newline or tab in `JIRA_MANAGEMENT_PROJECT_KEY`; JQL has `\n`/`\t`/`\uXXXX` escapes, and whether Jira accepts a raw newline inside a quoted literal is unverified — no live request was made, which the worker disclosed. Separately, `search_participation_issues`' docstring still says "Every issue in the caller's participation window" although the same 2000 cap applies | Real Jira project keys are `[A-Z][A-Z0-9_]+`, so every unescaped character is an operator-config hypothetical | Nothing today. Fix: validate the key against `^[A-Za-z][A-Za-z0-9_]*$` at the config boundary, which would make the escaper moot and close this and B144's edge together. Found by the T09 round-2 reviewer 2026-09-20 | S |
| B148 | **Any non-`SyncError` raised by a registered sync handler escapes `POST /api/sync` as a 500.** `run_sync` (`backend/app/api/sync.py`) catches only `SyncError`, and `app/main.py` registers handlers just for `FieldEncryptionError` and `StatementError`. Manager-reproduced 2026-09-20: a handler raising `GraphError("Microsoft Graph request failed")` returns **500**, while an unregistered kind correctly returns 409. Reachable on the default Graph path too — `graph.me()` in `_sync_graph` (`backend/app/services/sync.py`) is uncaught | Strictly pre-existing: the `try`/`except SyncError` block is byte-identical before and after T10's diff, which only adds the `kind` argument. T10's scope is the kind parameter, not the route's exception surface, and the reviewer classified it as a docstring-scoping blocker rather than a code defect | Any Jira or Graph transport failure — a 429, a 502, a DNS error — surfaces as an unhandled 500 with a stack trace rather than a clean 4xx/5xx envelope, which AC12's "never a stack trace" spirit argues against. Fix: catch `GraphError`/`JiraError` alongside `SyncError`, or give the connectors a shared base the route can catch. Found by the T10 reviewer, reproduced by the manager 2026-09-20 | M |
| B149 | **An empty `?kind=` answers 409 rather than falling back to the default**, and neither behaviour is pinned by a test. `kind: str = Query(default=GRAPH_SOURCE_KIND)` (`backend/app/api/sync.py`) treats a present-but-blank value as an explicit kind, so the caller gets `no sync handler registered for source kind ''` | AC11 asks that the parameter be optional and default to Graph, which it does when *omitted*; a blank value is a third case the task never named. The manager's M3 mutation (default → `""`) reddens, so the default itself is pinned | A client that builds the query string from an empty form field gets a confusing 409 instead of a Graph sync. Fix: decide whether blank means omitted, then pin it with a test either way. Found by the T10 reviewer 2026-09-20 | S |
| B150 | **The router tag rename is pinned by nothing.** `tags=["Microsoft Graph sync"]` → `tags=["source sync"]` (`backend/app/api/sync.py`) is OpenAPI-visible and outside the four changes T10's body named; mutating it to `tags=["mutated-tag"]` leaves the full suite at 372 passed (T10 reviewer), as does the manager's comment-only control | The manager accepted it explicitly: the route genuinely dispatches any registered kind after this diff, so leaving the Graph-only label would have been exactly the stale-sentence defect this contract has been bitten by eleven times. Reverting it would trade an untested rename for an untrue label | Nothing functional; the tag is documentation. Recorded so the next reader sees it was a deliberate manager call rather than unnoticed churn. Found by the T10 reviewer 2026-09-20 | S |
| B151 | **The backend suite leaves `/tmp/pytest-of-eimi/garbage-*` directories it fails to remove**, emitting `PytestWarning: (rm_rf) error removing ... Directory not empty` on every run, and `scripts/leave-no-trace.sh` reports `stray /tmp task files: 0` regardless — its matcher only admits names shaped `t<digits>`/`ac<digits>`, which `garbage-<uuid>` and `pytest-of-<user>` do not match | Pre-existing and not created by any task in this contract; AC4's gate is green as written, and widening the matcher is a change to the cleanup gate T01 owns rather than to T10 | The contract makes `/tmp` fatal on purpose, and this is a category of `/tmp` residue the gate is structurally blind to — the check reports zero while the directories accumulate. Fix: widen the matcher to `pytest-of-*`/`garbage-*`, or set `PYTEST_DEBUG_TEMPROOT` into `.companion/scratch/`. Found by the T10 reviewer 2026-09-20 | S |
| B152 | **AC13's verify line and T11's identical `done-when` can never exit 0, for any implementation.** `cd frontend` persists into the `python3 -c` that follows the `;`, so python resolves `.companion/scratch/ac13.json` against `frontend/` while vitest wrote it to the repo root via `--outputFile=../.companion/scratch/ac13.json`. Manager-measured 2026-09-20 **at base, before any T11 work, and again after it**: exit 1 with `FileNotFoundError`, while the artifact that same invocation wrote reads `numPassedTests=2, numFailedTests=0`. The counted assertion is the right idea — `-t` exits 0 on zero matches — and only its cwd is wrong | The contract is sealed: AC13 cannot be corrected without `/companion:contract` re-approval, and the manager must not rewrite the `done-when` it is graded by. A repair round cannot help — no worker change alters the outcome, and the only way to make the line pass as written would be to fabricate a copy of the report under `frontend/`, where python looks and nothing writes, which would make the gate meaningless | **T11 is blocked on this alone**; its code is merged, reviewed `approve` and green on every other criterion. `/companion:present` will fail AC13 the same way. Fix: `cd ..` before the `python3`, or give python the absolute path — one clause, no change to what is asserted | S |
| B153 | **`jiraIssueKey`'s `item.source_kind !== 'jira'` guard is pinned by nothing and unreachable today.** Manager mutation 2026-09-20: deleting the clause leaves the jira gate at 2 passed / 0 failed and the full frontend suite at 25 passed. The reviewer confirmed no non-Jira `source_url` the backend emits contains `/browse/` (Graph supplies `webLink`) | The shipped guard is correct and defensive; T11's body asks for the key on Jira items, not for a negative case on other kinds. Same class as B136 — code right, test missing | Nothing today. A future source whose URLs contain `/browse/` would silently render a bogus issue key. Fix: an `outlook_email` item with `source_url: 'https://mail.example/x/browse/OPS-9'` asserting `queryByRole('link', {name: 'OPS-9'})` is null. Found by the manager's matrix and the T11 reviewer 2026-09-20 | S |
| B154 | **The whole `sourceTone` map and the `mail-nav` dot-colour refactor are unpinned at runtime.** T11 replaced a hardcoded ternary with `sourceTone[kind]`; the reviewer mutated *both at once* — the lookup back to a constant `'bg-primary'` **and** `manual: 'bg-muted-foreground'` → `'bg-red-500'` — and the full suite stayed at 25 passed. `tsc -b` pins only that a `jira` key *exists* (manager-measured: removing it is `TS2741` against `Record<SourceKind, string>`), never which class any kind gets | The refactor is behaviour-preserving — `outlook_email` → `bg-sky-400` and `manual` → `bg-muted-foreground` are byte-identical to the ternary they replace, which the manager checked — and T11's body asks for the filter and the issue key, not for colour assertions. Accepted as knowingly untested churn, the same call recorded for B150 | Nothing functional; the dots are decoration. Fix: one assertion on the Jira dot's class in the filter test. Found by the T11 reviewer 2026-09-20 | S |
| B155 | **`scripts/leave-no-trace.sh`'s two-basename `/tmp` exemption rests on a premise the sealed contract contradicts.** It skips `ac13.json` and `t11.json` because "AC13's verify line writes `/tmp/ac13.json` on every run and task T11's gate writes `/tmp/t11.json` the same way" (script comment and `backend/tests/test_leave_no_trace_script.py:193-194`). Neither does: both write `../.companion/scratch/…`, which the manager confirmed by running each verbatim and watching where the file landed. The exemption's real effect is a permanent blind spot — `/tmp/ac13.json` was sitting there with an mtime of 2026-09-19 23:21 from an earlier contract, and the gate read `stray /tmp task files: 0` with it present | Pre-existing and owned by T01, not by T11: `scripts/leave-no-trace.sh` is byte-identical at T11's base. The exemption test builds its own fixtures under `LEAVE_NO_TRACE_TMP_DIR`, so it does not depend on the real file — the manager deleted `/tmp/ac13.json` and re-ran `-k leave_no_trace`: still 8 passed, gate still 0/0/0/0 | The owner's policy is that `/tmp` stays fatal and the cause gets fixed rather than the check softened — and this is a carve-out that outlived its cause, in the same category as B151. Fix: drop both exemptions now that nothing writes those paths, and correct the script comment and the test docstring. Found by the manager 2026-09-20 | S |
| B156 | **The two-line output shape is unpinned.** `docs/jira-setup.md` says the management-project line "only appears when `JIRA_MANAGEMENT_PROJECT_KEY` is set", but the only verbatim-output test sets that key, so adding an `else` branch that prints a third line when it is unset leaves `backend/tests/test_jira_check.py` at 13 passed (round-2 reviewer's M6, on the repaired module) | The three equality tests the repair added cover the documented healthy block and the unconfigured message, which is what round 1 blocked on; the unset-key *shape* is one case further out, and `test_run_check_omits_the_management_project_fields_when_unset` already pins the report data for it | The document's sentence about when the third line appears. Fix: one more `capsys` equality test with the key unset, asserting exactly two lines and `err == ""`. Found by the T12 round-2 reviewer 2026-09-20 | S |
| B157 | **`test_main_prints_a_request_failure_without_repeating_its_own_prefix` asserts stderr but not that stdout stayed empty**, so echoing the failure to stdout as well reddens only the unconfigured test (round-2 reviewer's M7) | The stream split is pinned for the unconfigured branch and for the healthy one; the request-failure branch is the third of three and the repair round was scoped to the three blocking findings | Nothing today — the shipped code writes to stderr only. Fix: add `assert captured.out == ""` to that test. Found by the T12 round-2 reviewer 2026-09-20 | S |
| B158 | **`README.md:31` cites `app/config.py:13` for `env_file=".env"`; it is line 31 today** (line 13 is `_BLANKABLE_OPTIONAL_FIELDS`). Manager-verified 2026-09-20. The sentence's substance — that the suite must run from `backend/` because the path resolves against the cwd — is true and is the same trap T12's own document had to work around | Outside T12's three files; the repair was already the one round the loop allows | Nothing functional. A reader who follows the citation lands on an unrelated tuple. Fix: `app/config.py:31`, or drop the line number. Found by the T12 repair worker 2026-09-20 | S |
