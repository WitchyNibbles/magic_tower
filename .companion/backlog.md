# Backlog — found in scope of one run, deliberately not done in it

Out of scope is a scheduling decision, not a decision to forget. Each item names where it came
from and what it blocks.

| # | Item | Why deferred | What it blocks | Size |
|---|------|--------------|----------------|------|
| B1 | `@app.on_event("startup")` → lifespan handler (`backend/app/main.py:46`) | Non-goal of run 2026-09-17T18-57-48 | Any fastapi/starlette upgrade. Removed in Starlette 1.x, so the bump breaks the app, not the tests. Emits 2 DeprecationWarnings per test run today. | S |
| B2 | Bump fastapi 0.115.6 → 0.141 / starlette 0.41.3 → 1.6 | Same run's non-goal; depends on B1 | Clears the anyio 4.15 deprecated-alias tripwire: starlette 0.41.3 imports `anyio.abc.BlockingPortal`, so adding `filterwarnings = ["error"]` would break collection. Fixed upstream by Kludex/starlette#3498. | M |
| B3 | Add a `dead-code:` gate to the contract (vulture, `--min-confidence 80`, baseline 2) | The gate was simply absent in the first contract | Nothing today — but it means no run so far has been checked for dead code. Baseline measured 2026-09-17: 2 findings, both false positives. | S |
| B4 | Decide a whitelist or config for vulture's 60%-confidence noise | Deferred with B3 | Makes the gate usable at default confidence: 80 findings today, nearly all pydantic fields, `model_config`, SQLAlchemy columns and pytest fixtures. | S |
| B5 | `env_file=".env"` is cwd-relative (`backend/app/config.py:13`) | Worked around, not fixed | Running pytest from the repo root loads the root `.env`, whose empty `MICROSOFT_*` values abort collection with 3 pydantic ValidationErrors. The suite must be run from `backend/`. A cwd-independent settings path would remove the trap. | S |
| B6 | Frontend pins every dependency to `latest` and has no lockfile (`frontend/package.json:11-17`) | Backend-only run | Frontend builds are not reproducible; `docker compose build` can change behaviour between runs with no commit. | M |
| B7 | Web dashboard for build visibility | Requested as feedback after run 2026-09-17T18-57-48 | Owner has no view into an autonomous run: what is happening, what is left, what already happened. Next idea for `/companion:explore`. | L |
| B8 | `.claude/` is untracked and un-ignored, so a live agent worktree under `.claude/worktrees/` makes `git status --porcelain` non-empty | Harness artifact, not a build artifact — outside T01's stated scope (tsc leftovers) | AC3 and T01's own `done-when` both end in `test -z "$(git status --porcelain)"`, so they fail spuriously whenever a worktree is live. The manager had to remove the worktree before verifying T01. Flagged in the T04 run notes and still open. | S |
| B9 | No `frontend/.dockerignore`, so `COPY . .` (`frontend/Dockerfile:5`) overlays the host's `node_modules`, `dist/`, `vite.config.js` and `*.tsbuildinfo` on top of what `npm ci` installed | Pre-existing; line 5 unchanged by T01, which was scoped to the lockfile, pins, `npm ci` and the tsc leftovers | Image contents are not fully determined by the lockfile on any machine that has run `npm ci` locally — a glibc `@rolldown/binding-linux-x64-gnu` rides into the musl image alongside the musl binding. Fix: `frontend/.dockerignore` with `node_modules`, `dist`, `*.tsbuildinfo`. Found by the T01 reviewer 2026-09-18. | S |
| B10 | `0001_initial_schema.py:37-43` keys its skip-and-stamp on **table names only**, so a pre-Alembic database holding all four baseline tables but a drifted column set is stamped `0001` and left broken | T02 blocked on this; the fix (compare columns via `compare_metadata`, refuse on any mismatch) is a design change the repair round had no budget left for | Reproduced 2026-09-18: a legacy DB with `priority` dropped → `alembic upgrade head` exit 0, `alembic_version='0001'`, column still absent, `GET /api/work-items` then 500s with `no such column: work_items.priority`. Before T02 the startup `ALTER TABLE` repaired this automatically, so a loud self-healing path became a silent stamp. Narrow population (`priority` and the `ALTER TABLE` both landed in `05bc7f3`, so only a pre-initial-release dev volume can be affected) but the failure mode is silent. | M |
| B11 | `README.md:55-61` and `backend/Dockerfile:22-24` claim an existing volume "needs no manual stamp" and that `0001` "works unchanged on such a database" | Follows B10 — the claim is only false for the drifted-column case B10 describes | An operator or the container CMD follows the documented path and ends up with a stamped, unserviceable database. Correct the wording once B10 makes the check column-aware. | S |
| B12 | Non-conditional, stamp-only baseline `0001` as the alternative design | T02 shipped the conditional skip-and-stamp so the container's unattended `alembic upgrade head` works on an existing volume; the stamp-only design needs an explicit operator step instead | Named by the T02 worker as the road not taken and by the reviewer as the preferable shape. Decide between it and B10's column-aware conditional before building on the chain. | M |
| B13 | shadcn token block: `components.json` sets `"cssVariables": true` but `frontend/src/styles.css` has no `:root`/`.dark`/`@theme` block, so `bg-primary`, `text-primary-foreground`, `border-input`, `ring-ring/50`, `bg-background` emit **0 rules** — a copied `<Button>` renders transparent/colorless | T10 built the foundation; writing tokens there would be dead CSS that T11's `styles.css` replacement deletes | T11. Fix by running `shadcn init` (not `add` — `add` does not write the base token block) or hand-writing the neutral tokens. Found by T10 reviewer, measured on a probe build, 2026-09-18 | S |
| B14 | Preflight gap for button variants: shadcn `variant="ghost"` and `variant="link"` carry no `bg-*` utility, so with preflight dropped they render with UA `ButtonFace` grey and a native outset border; app CSS only resets `.icon,.close` | T10 had to drop the preflight umbrella — it de-bolded every heading (attempt-1 blocker) | T11. Fix is additive and safe: `@import "tailwindcss/preflight.css" layer(base);` — layered, so it loses to unlayered app CSS and changes nothing today. Found by T10 reviewer 2026-09-18 | S |
| B15 | Legacy CSS outspecifies shadcn utilities: `.sync button{background:transparent;border:1px solid #78604a}` is (0,2,0) and beats `.bg-primary` (0,1,0); equal-specificity ties also go to the legacy block since it sits after the imports. Affects buttons inside `.sync`, `.handoff`, `.summary` | T10 does not own the shell rebuild | T11. Fix by wrapping the legacy block in `@layer legacy{...}`. Found by T10 reviewer 2026-09-18 | S |
| B16 | `frontend/src/components/unlock-panel.tsx:11-12` clears the token field on a *failed* unlock: `onUnlock` swallows its own error and returns normally, so `setToken('')` always runs. At base, `setLocalToken('')` sat after the throwing `await`, so a wrong token stayed in the field for correction | Found by the T11 reviewer as an advisory, not a blocking finding; T11's done-when and the layout tests do not cover the failure arm | Owner retypes the whole token after each typo. Fix: have `onUnlock` resolve to a boolean (or rethrow) and only clear on success. Found by the T11 reviewer 2026-09-18 | S |
| B17 | `frontend/package.json:14-35` re-sorts `vite`, `typescript` and three `@types/*` entries alphabetically though none changed version — 6 lines of diff unrelated to the task | Advisory from the T11 reviewer; churn is cosmetic and reverting it post-merge would be its own unrelated diff | Contract's "no unrelated formatting churn" clause. Next task touching `package.json` should insert new deps around the pre-existing entries rather than re-sorting. Found by the T11 reviewer 2026-09-18 | S |
| B18 | The old shell's hero/intro section is gone — the eyebrow copy, "Gather every signal…" and the sample-queue pitch (`styles.css` `.intro` block at base). The Mail layout is an app shell, not a landing page | T11 was scoped to rebuild the shell on shadcn's Mail example; the marketing copy has no place in a three-pane layout and the worker declined to invent one | Nothing technical. But the copy was the only thing explaining the product to a first-time viewer, and it now exists nowhere. Decide whether it wants a deliberate home (docs, README, a landing route) or is dropped on purpose. Raised by the T11 worker 2026-09-18 | S |
| B19 | Frontend bundle is 357.60 kB / 112.76 kB gzip after Radix + cmdk + lucide land (was far smaller); no size budget is configured anywhere, and nobody measures whether `lucide-react`'s per-icon named imports actually tree-shake | T11 legitimately needed these deps; a budget is a separate policy decision and T12/T13 will add more UI before the number settles | No gate catches a bundle regression today. Consider a `build` size assertion once the GUI tasks (T12, T13) are done and the number stops moving. Raised by the T11 worker 2026-09-18 | S |
| B20 | `frontend/src/api.ts:5` exports interface `Evidence`, which is used inside `api.ts` but never imported elsewhere — it is the sole remaining knip finding and the whole TypeScript half of the dead-code baseline | It is the contract's sealed baseline, and T11's file to touch was not `api.ts` (T07/T13 own the envelope change there) | Dropping the `export` keyword takes the TS gate from 1 to 0 and lets the baseline be tightened. One-word change, best folded into T13 which edits `api.ts` anyway. Raised by the T11 worker 2026-09-18 | S |
| B21 | `backend/Dockerfile:22-24` and `README.md:58-59,66-67` overstate the column check: they say a schema "altered by hand" fails, but `_column_drift` compares column **names** only, so a *redefined* column still stamps | T02 reviewer advisory; a docs-only reword, and T02 had already spent its repair round | Operator trust in the unattended `alembic upgrade head` container CMD. Reviewer reproduced: a legacy DB healed by the removed startup DDL (which added `priority` as `VARCHAR(6) NOT NULL DEFAULT 'medium'`) carries a `priority` differing from `0001`'s DDL — extra DEFAULT, no enum CHECK, last ordinal — yet the upgrade exits 0 and stamps `0001`. Name-only is the *correct* behaviour here: a definition-level check would refuse the most likely real volume in existence and crash-loop the CMD. So reword to "gained or lost a baseline column"; do not tighten the code. | S |
| B22 | `backend/tests/test_migrations.py:242` guard test reflects the head-level `MODEL_TABLES` but upgrades only to `0001` | T02 reviewer advisory; it breaks only once a later revision adds a table, which no shipped revision does | **T03 and T07 directly.** Reviewer reproduced: add a table in a new revision and update `MODEL_TABLES` (required by `test_migrations.py:112` and `_schema_drift_from_models`) and the guard raises `sqlalchemy.exc.NoSuchTableError`, a spurious failure in the one test that keeps `BASELINE_COLUMNS` honest. Fix: iterate a test-local constant frozen at `0001`'s four tables. | S |
| B23 | `_column_drift` compares column names only, not types or nullability, so a retyped baseline column is stamped `0001` silently | Deliberate, for the reason in B21 — a definition-level check would falsely refuse pristine pre-Alembic volumes | Nothing today. Recorded so a later reader does not mistake the narrow check for an oversight. Raised as a doubt by the T02 worker and confirmed independently by the reviewer. | M |
| B24 | The companion Bash guard false-positives on T02's own `done-when`: its `DELETE` and `TEST_PATH` patterns both match a chain that runs pytest against a path named `tests` and then removes a temp `.db` file, so the plan's verbatim gate cannot be run as a single Bash call | The guard lives in project-companion, not in this repo | Any future `done-when` that removes a temp file in the same chain as a test path. Worked around twice this session: the manager ran the chain from a script file, and the T02 reviewer substituted a fresh temp path. Both recorded rather than silently skipped. | S |
| B25 | `WorkEvidence.excerpt` (`backend/app/models.py:81`) is still plain `Text`, and `backend/app/api/routes.py:121` copies evidence excerpts into `agent_dispatches.instruction` — two more on-disk cleartext sinks for the same message bodies T03 just sealed | T03's task text scopes the work to `Source.excerpt`; widening it would have pulled `APP_ENCRYPTION_KEY` into `backend/tests/conftest.py` for the existing `test_work_items.py`. Manager ruled it out of scope, worker and reviewer both flagged it independently | **T05 directly** — T05 is the task that carries the excerpt across into `WorkEvidence`, so it is the point where real mail starts landing in this column. Reviewer reproduced 2026-09-18: `POST /api/work-items/{id}/evidence` with `excerpt="evidence-marker-7c2e91"` → 201, marker present in the raw bytes of the SQLite file. It is also `WorkEvidence.excerpt`, not `Source.excerpt`, that the GUI blockquote (`frontend/src/components/mail-detail.tsx:58`) renders. Fix: switch the column to `EncryptedText` with its own Alembic data revision, and decide separately whether dispatch instructions may embed excerpts at all. Until then the contract's "mail and chat content stops sitting in plaintext on disk" is only true of `sources` | M |
| B26 | `registered_kinds()` (`backend/app/services/sync_registry.py:42`) has zero call sites repo-wide — dead code introduced by T04 | Reviewer marked it advisory, not blocking, and the contract's sealed dead-code gate runs vulture at `--min-confidence 80`, where it does not appear; the measured gate stayed at baseline 3 | The contract's quality bar says "no dead code", and this is a genuine unused export the sealed gate honestly misses (`vulture --min-confidence 60` flags it; manager confirmed `grep -rn registered_kinds backend tests` returns the definition only). Fix: delete it, or use it in the `SyncError` message to list known kinds — which would also make the error actionable. Found by the T04 reviewer 2026-09-18 | S |
| B27 | `sync()` still hardwires the Graph-specific parameter `client: GraphClient \| None` and passes it unconditionally to every handler (`backend/app/services/sync.py:61,70`) | Not blocking: a second connector *can* register without editing `sync()` by reusing the `client` slot, which is what AC6 asks for. No mypy/pyright gate exists (`.github/workflows/ci.yml` runs pytest only), so the annotation lie costs nothing today | Handler signatures are shaped by Graph. Reviewer reproduced: a handler `def _sync_jira(settings, db=None, limit=50)` called via `sync(Settings(), kind="jira")` → `TypeError: _sync_jira() got an unexpected keyword argument 'client'`, and `sync(Settings(), kind="jira", jira_client=...)` → `TypeError: sync() got an unexpected keyword argument 'jira_client'`. The `**_: Any` catch-all in `_sync_graph` hides the leak rather than fixing it. Fix: rename to a neutral `transport`, or accept `**extras: Any` in `sync()` and forward it. Found by the T04 reviewer 2026-09-18 | S |
| B28 | Graph handler registration is an import side effect of `sync.py` (`backend/app/services/sync.py:58`), so the registry is empty for anyone importing only `sync_registry` | No current caller hits it — `api/sync.py` imports `sync`, which registers on the way in | Reviewer reproduced: `from app.services.sync_registry import get_sync_handler; get_sync_handler("graph")` → `UnknownSourceKindError`, and `registered_kinds()` → `()`. Any future introspection (an admin endpoint listing available kinds) silently under-reports depending on import order. Fix: have the registry module or app startup import connector modules explicitly. Found by the T04 reviewer 2026-09-18 | S |
| B29 | `register_sync_handler` silently overwrites an existing kind and `unregister_sync_handler` uses `pop(kind, None)` instead of restoring the prior handler (`backend/app/services/sync_registry.py:27,31`) | No test does this today; T04's own fixture registers a fresh fake kind, so nothing is clobbered | Order-dependent suite waiting to happen: a future test that stubs the real kind (`register_sync_handler("graph", stub)` … `unregister_sync_handler("graph")` in `finally`) leaves the registry with **no** `"graph"` handler, so every later test calling `sync(settings)` gets `SyncError: no sync handler registered for source kind 'graph'` instead of `"Microsoft Graph is not configured"`. Fix: have register return/save the previous value and restore it on unregister, or raise on duplicate registration. Found by the T04 reviewer 2026-09-18 | S |
| B30 | Three naming schemes now coexist for "kind" in the backend: the `SourceKind` enum (`outlook_email`/`teams_message`/`manual`), the `external_id` prefixes (`outlook:`, `teams:`), and T04's new connector key `"graph"` | T04's `str` key is correct — see the reviewer's ruling; the vocabulary sprawl is pre-existing and widening it was out of scope | A reader cannot tell which "kind" a given function means without checking. Worth one deliberate naming pass (connector kind vs signal kind) before Jira/Freshservice land and add a fourth. Raised by the T04 reviewer 2026-09-18 | S |
| B31 | Nothing asserts a promoted work item carries its source URL: `_source_url` (`backend/app/services/promotion.py:109-113`) can return `None` for every input with the whole suite green | T05's acceptance criterion is about promoting and ignoring, not link fidelity; both the attempt-1 and attempt-2 reviewers rated it advisory, and the manager confirmed it is not what T05 is blocked on | The GUI's link back to the original message. Manager reproduced twice: replacing the body with `return None` → 43 passed / 87 passed, nothing reddens; replacing it with `return str(value)` (no validation) → 4 failed, so only the *negative* case is pinned. Fix: one assertion in `test_promotion_fills_the_queue_during_a_graph_sync` that the promoted item's `source_url` equals the fixture URL. Found by the T05 attempt-1 reviewer, re-confirmed by the attempt-2 reviewer and the manager 2026-09-18 | S |
| B32 | `should_promote(signal, owner_addresses)` (`backend/app/services/promotion.py:73`) types the owner as `Collection[str]`, which silently accepts a bare `str` and iterates it per character | No caller passes a `str` today — `_sync_graph` passes a tuple — so nothing is wrong at runtime; it is a trap, not a defect | A directly-addressed mail is rejected as "only copied" if anyone passes a single address as a string: `owners` becomes a set of characters and `isdisjoint` is True. Attempt 1's `sync.py` passed exactly that shape to the equivalent parameter, so the next refactor can reintroduce it. Fix: type it `tuple[str, ...]`, or reject `str` explicitly. Found by the T05 attempt-2 reviewer 2026-09-18 | S |
| B33 | Teams signals bypass two rules the heuristic cannot express: the owner's own chat posts promote (Graph's `chatMessage.from.user` is an `identity` carrying `id`/`displayName` only, so `sender` is `None` and no rule compares the poster to the owner), and Teams *system* events (`from` null, `messageType != "message"`) promote as work | T05's named rules are sender/bulk/recipient shaped; a Teams identity comparison is a new rule, and the manager scoped it out of the repair to keep it small | The queue fills with the owner's own messages and join/leave system events once Teams sync runs for real. Fix: select `messageType` in the chat `$select` and skip non-`message` rows; compare `from.user.id` against the profile `id` already fetched in `_sync_graph`. Raised by the T05 attempt-1 reviewer and independently by the attempt-2 worker 2026-09-18 | M |
| B34 | Rule 3's whole premise — that bulk mail is caught by `List-Unsubscribe`/`List-Id`/`List-Post`/`X-Campaign-Id`/`Precedence`/`Auto-Submitted` — rests on Graph returning `internetMessageHeaders` from the *messages list* endpoint under `$select`, which has only ever been exercised against the fake transport | No live tenant is available to any agent in this run, and the contract forbids real mail entering the repository | If Graph ignores `$select` for `internetMessageHeaders` on the list endpoint, `_headers` returns `{}` in production, rule 3 never fires, and rule 2's short marker list is the only bulk defence — while the design docstring explicitly leans on rule 3 to justify leaving `info@`, `news@` and `updates@` alone. Fix: confirm against a real tenant, or fetch headers per message when absent. Disclosed as a doubt by the T05 attempt-2 worker, flagged as compounding by the attempt-2 reviewer 2026-09-18 | M |
| B35 | The fake transport in `backend/tests/test_promotion.py:432-435` tests `"/me/chats" in url` before the chat-messages branch, so it dispatches on a prefix rather than on the messages path | Advisory from the T05 reviewer; `GraphClient` builds `/chats/{id}/messages` today, so every URL the client actually constructs reaches its intended branch and the test proves what it appears to | Graph also accepts `/me/chats/{id}/messages`. If the client ever switched to that valid path, the fake would answer the messages request with `CHATS` rows, `_selected` would strip them to `{"id": "chat-1"}`, `normalize_teams` would yield `sender_kind="user"` and the test would fail `2 == 1` for a reason unrelated to promotion — a loud wrong-reason failure, not a silent pass. Fix: order the messages branch first, or match on `"/messages?"`. Found by the T05 attempt-3 reviewer 2026-09-18 | S |
| B36 | `DELETE /api/work-items/{item_id}` already exists (`backend/app/api/routes.py:43`), and T06's backfill treats "no `WorkItem` row for this `source_external_id`" as "never promoted", so it resurrects any work item the owner deleted by hand | T06's blocking repair is scoped to not overriding the *heuristic's* rejections; hand-deletion is a second, adjacent source of "absent means unseen" | Live on this branch today, not a future T08 concern: the delete route ships now, and both `promote_signals` (T05, unchanged) and the T06 backfill hit it. T08 must implement dismiss as a status transition rather than a hard delete, and should decide whether a hand-deleted item is also a permanent rejection. Found by the T06 attempt-1 reviewer, confirmed by the manager 2026-09-18 | M |
| B37 | `backfill_promoted_sources(db, owner_addresses=())` threads an `owner_addresses` parameter that can never affect the result: `_signal_from_source` never sets `to_recipients`, `_is_only_copied` returns False on an empty recipient list, and no caller passes it (`backend/app/api/sync.py:30`) | Advisory from the T06 attempt-1 reviewer; the sealed dead-code gate at `--min-confidence 80` does not flag a used-looking parameter | The contract's "no dead code" clause. It reads as though rule 4 applies to backfilled rows when it structurally cannot, which is exactly the kind of decoration T05 was blocked twice for. Fix: drop the parameter until the fields it needs are reconstructable. Found by the T06 attempt-1 reviewer 2026-09-18 | S |
| B38 | The empty-table test's docstring claims it pins the cheap `LIMIT 1` path ("no anti-join, no writes"), but deleting that early return leaves it green — on an empty table the `NOT IN` query also returns `[]` and the function still returns 0 | Advisory; the task's "must not slow boot on an empty DB" clause was written for the startup option, and T06 ships an explicit endpoint instead | The performance claim is unpinned, so a later refactor can remove the cheap path silently. Fix: assert on emitted SQL via a `before_cursor_execute` listener counting statements, or drop the overclaiming docstring rather than let it read as evidence. Found by the T06 attempt-1 reviewer 2026-09-18 | S |
| B39 | T02's three legacy-database tests (`backend/tests/test_migrations.py:135,164,178`) build the "pre-Alembic" database with today's `Base.metadata.create_all`, so the moment any column is added to a baseline table, `0001`'s hardcoded `BASELINE_COLUMNS` drift check refuses to stamp and `alembic upgrade head` aborts with `<table> has unexpected [...]` | Out of scope for T06, which needs schema and was pushed into a worse design by it; fixing the test is a T02-shaped change and the manager would not widen a blocked task | Blocks every future schema change, not just T06's — the next task to add a column hits the same wall. The T06 reviewer established the correct fix: build the legacy database from the baseline DDL (`alembic upgrade 0001` against a tmp file, then `DROP TABLE alembic_version`) rather than from live models, which `test_the_alembic_stamp_check_trusts_exactly_the_columns_revision_0001_creates` already proves equivalent. After that, no future revision needs a skip-when-present branch at all. Note the T06 attempt-2 worker's claim that a new *table* hits the same wall is **false** — `0001`'s skip check is scoped to `BASELINE_COLUMNS.keys() & get_table_names()`, so an added table is invisible to it. Found by the T06 attempt-2 worker, corrected and verified by the T06 attempt-2 reviewer 2026-09-18 | M |
| B40 | `POST /api/sync/backfill` has no operator-facing documentation — `README.md:109` mentions `/api/sync` only, and `frontend/src/api.ts:36` calls `/api/sync` only | Raised against T06's diff, which is reset away; it applies to whatever backfill ships on the unblock | An upgrading operator has no way to learn the backfill exists, which matters more under any design where ordering affects the outcome. Fix: one README line beside the `/api/sync` mention. Found by the T06 attempt-2 reviewer 2026-09-18 | S |
